/**
 * Paysecure API Documentation - LiveLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  CheckingBalancePayoutSuccess,
  checkingBalancePayoutSuccessSchema,
} from '../models/checkingBalancePayoutSuccess.js';
import {
  CheckingBalanceRequest,
  checkingBalanceRequestSchema,
} from '../models/checkingBalanceRequest.js';
import { Success24, success24Schema } from '../models/success24.js';
import { Success6, success6Schema } from '../models/success6.js';
import { BaseApi } from './baseApi.js';
import { FailureError } from '../errors/failureError.js';

export class GetBalanceApi extends BaseApi {
  /**
   * ## **API for checking available balance**
   *
   * 1. URL :- /getBalance/
   *
   * 2. RequestType: POST
   *
   * 3. Request Body
   *
   *
   * Note: Currency is not a mandatory parameter. If no parameter is specified then all respective
   * currency balances would be provided, if any specific currency parameter is provided then only that
   * currency balance would be provided.
   *
   * @param body
   * @return Response from the API call
   */
  async checkingBalance(
    body: CheckingBalanceRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CheckingBalancePayoutSuccess>> {
    const req = this.createRequest('POST', '/api/v1/getBalance');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, checkingBalanceRequestSchema],
    });
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(checkingBalancePayoutSuccessSchema, requestOptions);
  }

  /**
   * ## **API for Checking Balance payout**
   *
   * 1. URL :- /getBalance/
   *
   * 2. RequestType: POST
   *
   * 3. Request Body
   *
   *
   * Note: Currency is not a mandatory parameter. If no parameter is specified then all respective
   * currency balances would be provided, if any specific currency parameter is provided then only that
   * currency balance would be provided.
   *
   * @param body
   * @return Response from the API call
   */
  async checkingBalance1(
    body: CheckingBalanceRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success6>> {
    const req = this.createRequest('POST', '/api/v1/getBalance');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, checkingBalanceRequestSchema],
    });
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success6Schema, requestOptions);
  }

  /**
   * ## **API for Checking Balance payout**
   *
   * 1. URL :- /getBalance/
   *
   * 2. RequestType: POST
   *
   * 3. Request Body
   *
   *
   * Note: Currency is not a mandatory parameter. If no parameter is specified then all respective
   * currency balances would be provided, if any specific currency parameter is provided then only that
   * currency balance would be provided.
   *
   * @param body
   * @return Response from the API call
   */
  async checkingBalance2(
    body: CheckingBalanceRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CheckingBalancePayoutSuccess>> {
    const req = this.createRequest('POST', '/api/v1/getBalance');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, checkingBalanceRequestSchema],
    });
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(checkingBalancePayoutSuccessSchema, requestOptions);
  }

  /**
   * ## **API for Checking Balance payout**
   *
   * 1. URL :- /getBalance/
   *
   * 2. RequestType: POST
   *
   * 3. Request Body
   *
   *
   * Note: Currency is not a mandatory parameter. If no parameter is specified then all respective
   * currency balances would be provided, if any specific currency parameter is provided then only that
   * currency balance would be provided.
   *
   * @param body
   * @return Response from the API call
   */
  async checkingBalance3(
    body: CheckingBalanceRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success24>> {
    const req = this.createRequest('POST', '/api/v1/getBalance');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, checkingBalanceRequestSchema],
    });
    req.json(mapped.body);
    req.throwOn(400, FailureError, 'Bad Request');
    req.authenticate([]);
    return req.callAsJson(success24Schema, requestOptions);
  }
}
