/**
 * Paysecure API Documentation - LiveLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  CreatePayOutRequest,
  createPayOutRequestSchema,
} from '../models/createPayOutRequest.js';
import {
  CreatePayOutRequest1,
  createPayOutRequest1Schema,
} from '../models/createPayOutRequest1.js';
import {
  CreatePayOutRequest3,
  createPayOutRequest3Schema,
} from '../models/createPayOutRequest3.js';
import {
  CreatePayOutRequest4,
  createPayOutRequest4Schema,
} from '../models/createPayOutRequest4.js';
import {
  CreatePayOutRequest6,
  createPayOutRequest6Schema,
} from '../models/createPayOutRequest6.js';
import {
  CreatePayoutSuccess,
  createPayoutSuccessSchema,
} from '../models/createPayoutSuccess.js';
import {
  CreatePayoutSuccess1,
  createPayoutSuccess1Schema,
} from '../models/createPayoutSuccess1.js';
import {
  CreatePayoutSuccess2,
  createPayoutSuccess2Schema,
} from '../models/createPayoutSuccess2.js';
import { GetStatus, getStatusSchema } from '../models/getStatus.js';
import {
  GetSuccessStatus,
  getSuccessStatusSchema,
} from '../models/getSuccessStatus.js';
import {
  GetSuccessStatus1,
  getSuccessStatus1Schema,
} from '../models/getSuccessStatus1.js';
import {
  GetSuccessStatus2,
  getSuccessStatus2Schema,
} from '../models/getSuccessStatus2.js';
import { NewRequest, newRequestSchema } from '../models/newRequest.js';
import {
  NewRequestRequest,
  newRequestRequestSchema,
} from '../models/newRequestRequest.js';
import {
  PayoutApIsRequest,
  payoutApIsRequestSchema,
} from '../models/payoutApIsRequest.js';
import {
  PayoutApIsRequest1,
  payoutApIsRequest1Schema,
} from '../models/payoutApIsRequest1.js';
import {
  PayoutRequest2,
  payoutRequest2Schema,
} from '../models/payoutRequest2.js';
import { Success12, success12Schema } from '../models/success12.js';
import { Success18, success18Schema } from '../models/success18.js';
import { Success23, success23Schema } from '../models/success23.js';
import { Success5, success5Schema } from '../models/success5.js';
import { Success8, success8Schema } from '../models/success8.js';
import { string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { FailureError } from '../errors/failureError.js';

export class PayoutApIsApi extends BaseApi {
  /**
   * To Initiate a payment, the very first call to make is /payout/
   *
   * with the required data in the request body.
   *
   * To generate a Payout, you are required to provide the `API key` (in the header) can be located in
   * the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a Payout
   * request.
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.email | An Identifier for a user |
   * | client.full_name | An Identifier for a user |
   * | client.country | CA |
   * | payoutMethod | PAYOUT-INTERAC-ETRANSFER |
   * | Currency | CAD |
   * | Amount | Payout Amount |
   * | Purpose | Transfer Reversed |
   *
   * @param brandId
   * @param body
   * @return Response from the API call
   */
  async createPayOut(
    brandId: string,
    body: CreatePayOutRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CreatePayoutSuccess>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      brandId: [brandId, string()],
      body: [body, createPayOutRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.header('brandId', mapped.brandId);
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(createPayoutSuccessSchema, requestOptions);
  }

  /**
   * This API tells you about all the details of a Payout, including its history
   *
   * ### Mandatory
   *
   * you need to pass the payoutId in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | paid | Transaction Successful |
   * | payout_in_process | payout is under Processing. |
   * | error | Transaction has Failed. |
   *
   * The webhook will only be triggered for paid transactions or errors.
   *
   * Paid: "success_callback": "[https://success.com](https://success.com)",
   *
   * Error: "failure_callback": “[https://failure.com”](https://failure.com”)
   *
   * @param payoutId
   * @return Response from the API call
   */
  async getStatus(
    payoutId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<GetSuccessStatus>> {
    const req = this.createRequest('GET');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ payoutId: [payoutId, string()] });
    req.appendTemplatePath`/api/v1/getpayout/${mapped.payoutId}`;
    req.authenticate([]);
    return req.callAsJson(getSuccessStatusSchema, requestOptions);
  }

  /**
   * To Initiate a payment, the very first call to make is /payout/
   *
   * with the required data in the request body.
   *
   * To generate a Payout, you are required to provide the `API key` (in the header) can be located in
   * the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a Payout
   * request.
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.email or client.phone | An Identifier for a user |
   * | client.full_name | Full Name of the user |
   * | client.country | CA |
   * | payoutMethod | PAYOUT-INTERAC-REQUEST-MONEY |
   * | Currency | Note this is currency account for which you have balance |
   * | Amount | amount in CAD |
   * | Purpose | eg. "Transfer Reversed" |
   *
   * @param brandId
   * @param body
   * @return Response from the API call
   */
  async createPayOut1(
    brandId: string,
    body: CreatePayOutRequest1,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success5>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      brandId: [brandId, string()],
      body: [body, createPayOutRequest1Schema],
    });
    req.header('Content-Type', 'application/json');
    req.header('brandId', mapped.brandId);
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success5Schema, requestOptions);
  }

  /**
   * This API tells you about all the details of a Payout, including its history
   *
   * ### Mandatory
   *
   * you need to pass the payoutId in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | paid | Transaction Successful |
   * | payout_in_process | payout is under Processing. |
   * | error | Transaction has Failed. |
   *
   * The webhook will only be triggered for paid transactions or errors.
   *
   * Paid: "success_callback": "[https://success.com](https://success.com)",
   *
   * Error: "failure_callback": “[https://failure.com”](https://failure.com”)
   *
   * @param payoutId
   * @return Response from the API call
   */
  async getStatus1(
    payoutId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<GetSuccessStatus1>> {
    const req = this.createRequest('GET');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ payoutId: [payoutId, string()] });
    req.appendTemplatePath`/api/v1/getpayout/${mapped.payoutId}`;
    req.authenticate([]);
    return req.callAsJson(getSuccessStatus1Schema, requestOptions);
  }

  /**
   * To Initiate a payment, the very first call to make is /payout/
   *
   * with the required data in the request body.
   *
   * To generate a Payout, you are required to provide the `API key` (in the header) can be located in
   * the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a Payout
   * request.
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.email client.phone | An Identifier for a user |
   * | client.full_name | An Identifier for a user |
   * | client.country | CA |
   * | payoutMethod | PAYOUT-INTERAC-EXPRESS |
   * | Currency | CAD |
   * | Amount | Payout Amount |
   * | Purpose | Transfer Reversed |
   *
   * @param brandId
   * @param body
   * @return Response from the API call
   */
  async createPayout2(
    brandId: string,
    body: CreatePayOutRequest1,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CreatePayoutSuccess1>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      brandId: [brandId, string()],
      body: [body, createPayOutRequest1Schema],
    });
    req.header('Content-Type', 'application/json');
    req.header('brandId', mapped.brandId);
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(createPayoutSuccess1Schema, requestOptions);
  }

  /**
   * To Initiate a payout, the very first call to make is /payout/ API with the required data in the
   * request body.
   *
   * To generate a PayOut, you are required to provide the `API key` (in the header), which can be
   * located in the Dashboard section of your merchant account login.
   *
   * The following (see table below) are the mandatory parameters that are required to create a payout
   * request.
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.email | The customer's email. |
   * | client.phone | The customer's phone number along with the country code.  <br>Example: +91
   * 9634088651 |
   * | client.city | The customer's city. |
   * | client.country | ISO-3166 Country Code. Must be upper case. Example “SG” (Alpha2) |
   * | client.stateCode | Example “AL”, “XZ”. Must be in upper case. |
   * | Client.street_address | The customer's address. |
   * | client.zip_code | The customer's ZIP or postal code. If country=US, zip format must be NNNNN or
   * NNNNN-NNNN. |
   * | currency | ISO 4217 code for currency you want to send the transaction in.  <br>  <br>Please note,
   * the currency has to be enabled by the account manager for your account. |
   * | amount | amount in decimal format.  <br>example 1:  <br>EUR 5 , should be sent as 5.00  <br>
   * <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.37 |
   * | payoutMethod | example: PAYOUT-PIX |
   * | success_redirect | URL to send the user if the transactions is successful. |
   * | pending_redirect | URL to send the user if the transactions is in pending. |
   * | failure_redirect | URL to send the user if the transactions is unsuccessful. |
   * | Purpose | The purpose of doing a payout. |
   *
   * ### Additional **parameters for this usecase:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.documentType | Mandatory.  <br>Document Type Supported : cpf, cnpj, evp, email, phone  <br>
   * <br>cpf is Brazil's tax_id  <br>cnpj is Business tax_id  <br>evp is random key generated by the
   * bank  <br>email is the email which supports pix  <br>phone is the phone number which supports pix |
   * | client.documentId | Mandatory.  <br>Document value of the type above. |
   *
   * @param body
   * @return Response from the API call
   */
  async createPayOut3(
    body: CreatePayOutRequest3,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success8>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, createPayOutRequest3Schema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success8Schema, requestOptions);
  }

  /**
   * This API tells you about all the details of a Payout, including its history
   *
   * ### Mandatory
   *
   * you need to pass the payoutId in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | paid | Transaction Successful |
   * | payout_in_process | payout is under Processing. |
   * | error | Transaction has Failed. |
   *
   * The webhook will only be triggered for paid transactions or errors.
   *
   * Paid: "success_callback": "[https://success.com](https://success.com)",
   *
   * Error: "failure_callback": “[https://failure.com”](https://failure.com”)
   *
   * @param payoutId
   * @return Response from the API call
   */
  async getStatus2(
    payoutId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success8>> {
    const req = this.createRequest('GET');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ payoutId: [payoutId, string()] });
    req.appendTemplatePath`/api/v1/getpayout/${mapped.payoutId}`;
    req.authenticate([]);
    return req.callAsJson(success8Schema, requestOptions);
  }

  /**
   * To Initiate a payment, the very first call to make is/payout/
   *
   * with the required data in the request body.
   *
   * To generate a Payout, you are required to provide the `API key` (in the header) can be located in
   * the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a Payout
   * request.
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.email or client.phone | An Identifier for a user |
   * | client.full_name |  |
   * | client.country |  |
   * | payoutMethod |  |
   * | Currency | Note this is currency account for which you have balance |
   * | Amount |  |
   * | Purpose |  |
   *
   * @param body
   * @return Response from the API call
   */
  async createPayOut11(
    body: CreatePayOutRequest4,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CreatePayoutSuccess2>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, createPayOutRequest4Schema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(createPayoutSuccess2Schema, requestOptions);
  }

  /**
   * This API tells you about all the details of a Payout, including its history
   *
   * ### Mandatory
   *
   * you need to pass the payoutId in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | paid | Transaction Successful |
   * | payout_in_process | payout is under Processing. |
   * | error | Transaction has Failed. |
   *
   * The webhook will only be triggered for paid transactions or errors.
   *
   * Paid: "success_callback": "[https://success.com](https://success.com)",
   *
   * Error: "failure_callback": “[https://failure.com”](https://failure.com”)
   *
   * @param payoutId
   * @return Response from the API call
   */
  async getStatus3(
    payoutId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<GetSuccessStatus2>> {
    const req = this.createRequest('GET');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ payoutId: [payoutId, string()] });
    req.appendTemplatePath`/api/v1/getpayout/${mapped.payoutId}`;
    req.authenticate([]);
    return req.callAsJson(getSuccessStatus2Schema, requestOptions);
  }

  /**
   * **Payout APIs Integration (SPEI)**
   *
   * ---
   *
   * #### 1\. Request Payload Structure
   *
   * Submit a JSON payload containing:
   *
   * - `client` (beneficiary details)
   *
   * - `purpose` (should be `"payout"`)
   *
   * - `currency` (always `"MXN"`)
   *
   * - `payoutMethod` (must be `"PAYOUT-SPEI"`)
   *
   * - `amount` (numeric, in major units)
   *
   * - Redirect & callback URLs
   *
   *
   * ``` json
   * jsonCopy{
   * "client": {
   * "email": "ashishm.21190@gmail.com",
   * "country": "MX",
   * "phone": "+14377717874",
   * "full_name": "MARCO ANTONIO FERNANDEZ RAMIREZ",
   * "stateCode": "test",
   * "spei_clabe": "012180027944986158"
   * },
   * "purpose": "payout",
   * "currency": "MXN",
   * "payoutMethod": "PAYOUT-SPEI",
   * "amount": 20,
   * "success_redirect": "https://test.com/getResponse.jsp?success=true",
   * "failure_redirect": "https://test.com/getResponse.jsp?success=false",
   * "success_callback": "https://test1.com",
   * "failure_callback": "https://test2.com"
   * }
   *
   * ```
   *
   * - **client.email**, **client.phone**, **client.full_name**, **client.country**, **client.stateCode**:
   * Required beneficiary/contact info.
   *
   * - **client.spei_clabe**: 18-digit CLABE of beneficiary’s bank account (validate format client-side
   * to avoid runtime failures).
   *
   * - **purpose**: Always set to `"payout"`.
   *
   * - **currency**: Must be `"MXN"`.
   *
   * - **payoutMethod**: Must be `"PAYOUT-SPEI"`.
   *
   * - **amount**: Numeric amount (e.g., `20` for MXN 20.00).
   *
   * - **success_redirect** / **failure_redirect**: URLs for end-user redirect after they click “Return
   * to Merchant” (GET).
   *
   * - **success_callback** / **failure_callback**: Webhook endpoints for asynchronous payout status
   * notifications.
   *
   *
   * ---
   *
   * #### 2\. Sample Response & Key Fields
   *
   * ``` json
   * jsonCopy{
   * "payoutId": "683d778dc74d23074a56ea94",
   * "client": {
   * "customerId": "NA",
   * "email": "ashishm.21190@gmail.com",
   * "phone": "+14377717874",
   * "full_name": "MARCO ANTONIO FERNANDEZ RAMIREZ",
   * "country": "MX",
   * "spei_clabe": "012180027944986155",
   * "stateCode": "test"
   * },
   * "updated_on": 1748858765,
   * "payoutMethod": "PAYOUT-SPEI",
   * "amountUnit": "MAJOR",
   * "amount": 20.0,
   * "errorMsg": "This customer can not be processed !",
   * "purpose": "test payout",
   * "created_on": 1748858765,
   * "merchantRef": "683d778dc74d23074a56ea94",
   * "status": "ERROR",
   * "status_history": [
   * {
   * "status": "created",
   * "timestamp": 1748858765
   * },
   * {
   * "status": "error",
   * "timestamp": 1748858765
   * }
   * ],
   * "currency": "MXN",
   * "paymentOn": 0,
   * "taxAmount": 0.0,
   * "taxPercent": 0.0,
   * "success_redirect": "https://test.com/getResponse.jsp?success=true",
   * "failure_redirect": "https://test.com/getResponse.jsp?success=false",
   * "redirectType": "GET",
   * "success_callback": "https://test1.com",
   * "failure_callback": "https://test2.com"
   * }
   *
   * ```
   *
   * - **payoutId** (`string`): Unique PaySecure identifier for this payout request.
   *
   * - **client.customerId**: `"NA"` for a new beneficiary or your internal customer ID if returning.
   *
   * - **client.spei_clabe**: Echoed beneficiary CLABE (note: if the CLABE in response differs by a digit,
   * double-check formatting/validation).
   *
   * - **updated_on / created_on**: Unix timestamps (seconds since epoch).
   *
   * - **payoutMethod**: `"PAYOUT-SPEI"`.
   *
   * - **amountUnit**: `"MAJOR"` indicates amount is in MXN major units (no subunit conversion required).
   *
   * - **amount**: `20.0` (MXN 20.00).
   *
   * - **errorMsg**: Describes any immediate rejection reason. If non-empty, `status` will be `"ERROR"`.
   *
   * - **purpose**: Echo of the `"payout"` field or any custom string you passed.
   *
   * - **merchantRef**: Mirror of `payoutId` by default; can be overwritten if you pass a custom
   * reference.
   *
   * - **status**: One of:
   *
   * - `"created"` → request accepted, preparing SPEI transfer
   *
   * - `"processing"` → SPEI transfer initiated, awaiting Banco de México settlement
   *
   * - `"success"` → funds successfully delivered
   *
   * - `"error"` → immediate failure (e.g., invalid CLABE, validation issue)
   *
   * - **status_history**: Array showing timestamped states from `"created"` → `"processing"` →
   * (`"success"` or `"error"`).
   *
   * - **paymentOn**: Unix timestamp when the transfer actually occurred (0 if not delivered).
   *
   * - **redirectType**: Always `"GET"`, meaning customer is redirected via HTTP GET to your provided URL.
   *
   * - **success_redirect** / **failure_redirect** / **success_callback** / **failure_callback**: Echo of
   * whatever you passed in request.
   *
   *
   * **Note:** If you see `"status": "ERROR"` and an `errorMsg`, do not retry automatically—inspect the
   * message, correct client data, then reinitiate a new payout request.
   *
   * ---
   *
   * #### 3\. Handling Payout Webhooks & Status Flow
   *
   * 1. **Configure Webhooks**
   *
   * - In your PaySecure Dashboard, register your `success_callback` and `failure_callback` URLs
   * (these will receive POSTs once SPEI settles or fails).
   *
   * 2. **Listen for Events**
   *
   * - `payout.success`: Banco de México confirmed the credit to beneficiary’s CLABE.
   *
   * - `payout.error`: CLABE invalid, account closed, or transfer rejected.
   *
   * 3. **Update Your Records**
   *
   * - On `payout.success`: Mark disbursement as complete; notify your finance/operations teams.
   *
   * - On `payout.error`: Log failure details, alert operations for manual review, and optionally
   * retry after fixing the root cause.
   *
   * @param body
   * @return Response from the API call
   */
  async payoutApIs(
    body: PayoutApIsRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success12>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ body: [body, payoutApIsRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success12Schema, requestOptions);
  }

  /**
   * To Initiate a payout, the very first call to make is /payout/ API with the required data in the
   * request body.
   *
   * To generate a PayOut, you are required to provide the `API key` (in the header), which can be
   * located in the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a purchase
   * request.
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.full_name | Full name of the receiver. Minimum 2 words with atleast 1 character in each
   * word. |
   * | client.email | An Identifier for a receiver, should be a valid email. |
   * | client.country | ISO-3166 Country Code. It must be upper case. |
   * | client.stateCode | ISO-3166-2 State Code. Must be in upper case. |
   * | client.street_address | street address of the receiver. |
   * | client.zip_code | zip code of the receiver. |
   * | client.phone | Phone number of the receiver. Country code is mandatory.  <br>Example:
   * +234999999999 |
   * | payoutMethod | Payout-MOBILEMONEY |
   * | Amount | Amount in currency upto 2 decimal points |
   * | Currency | Note this is currency account for which you have balance. |
   * | success_redirect | URL to send the user if the payout is successful |
   * | pending_redirect | URL to send the user if the payout is in progress |
   * | failure_redirect | URL to send the user if the payout is unsuccessful. |
   * | Purpose | Purpose of Payout |
   *
   * @param body
   * @return Response from the API call
   */
  async payout(
    body: PayoutRequest2,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<string>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ body: [body, payoutRequest2Schema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsText(requestOptions);
  }

  /**
   * ### Create Payout
   *
   * This endpoint allows you to initiate a payout transaction.
   *
   * #### Request Body
   *
   * - `client` (object, required): Details of the client initiating the payout.
   *
   * - `email` (string): The email of the client.
   *
   * - `country` (string): The country of the client.
   *
   * - `phone` (string): The phone number of the client.
   *
   * - `full_name` (string): The full name of the client.
   *
   * - `stateCode` (string): The state code of the client.
   *
   * - `purpose` (string, required): The purpose of the payout.
   *
   * - `currency` (string, required): The currency of the payout.
   *
   * - `payoutMethod` (string, required): The method of payout.
   *
   * - `amount` (number, required): The amount of the payout.
   *
   * - `success_callback` (string, required): The URL to which the success callback will be sent.
   *
   * - `failure_callback` (string, required): The URL to which the failure callback will be sent.
   *
   *
   * #### Response Body
   *
   * The response will contain the details of the initiated payout transaction.
   *
   * @param body
   * @return Response from the API call
   */
  async payoutApIs1(
    body: PayoutApIsRequest1,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NewRequest>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ body: [body, payoutApIsRequest1Schema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(newRequestSchema, requestOptions);
  }

  /**
   * ## Payout Request API
   *
   * This endpoint allows clients to initiate a payout transaction. It requires specific details about
   * the client and the payout method to process the request successfully.
   *
   * ### Request Format
   *
   * The request must be sent as a JSON object in the raw body. The following parameters are expected:
   *
   * - **client**: An object containing client information.
   *
   * - **email** (string): The email address of the client.
   *
   * - **country** (string): The country of the client.
   *
   * - **phone** (string): The phone number of the client.
   *
   * - **full_name** (string): The full name of the client.
   *
   * - **purpose** (string): A brief description of the reason for the payout (e.g., "testing payout").
   *
   * - **currency** (string): The currency in which the payout is to be made (e.g., "AUD").
   *
   * - **payoutMethod** (string): The method used for the payout (e.g., "PAYOUT-PAYID").
   *
   * - **amount** (number): The amount to be paid out.
   *
   * - **extraParam**: An object containing additional payout parameters.
   *
   * - **payId** (string): The unique identifier for the payout.
   *
   * - **bankName** (string): The name of the bank associated with the payout.
   *
   * - **accountName** (string): The name on the account receiving the payout.
   *
   * - **success_callback** (string): A URL that will be called upon successful completion of the payout.
   *
   * - **failure_callback** (string): A URL that will be called if the payout fails.
   *
   *
   * ### Response Structure
   *
   * The response from this endpoint will typically include details about the status of the payout
   * request, which may include:
   *
   * - A success message or error details.
   *
   * - A transaction ID for tracking purposes.
   *
   * - Any additional information relevant to the payout process.
   *
   *
   * Ensure to handle both success and failure callbacks appropriately based on the response received
   * from this endpoint.
   *
   * @param body
   * @return Response from the API call
   */
  async newRequest(
    body: NewRequestRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success18>> {
    const req = this.createRequest('POST', '/>');
    req.baseUrl('Server 1');
    const mapped = req.prepareArgs({ body: [body, newRequestRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success18Schema, requestOptions);
  }

  /**
   * To Initiate a payment, the very first call to make is /payout/ with the required data in the request
   * body.
   *
   * To generate a Payout, you are required to provide the `API key` (in the header) can be located in
   * the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a Payout
   * request.
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.full_name | Full name of the receiver. Minimum 2 words with atleast 1 character in each
   * word. |
   * | client.email | An Identifier for a receiver, should be a valid email. |
   * | client.country | ISO-3166 Country Code. It must be upper case. |
   * | client.stateCode | State of the receiver. Must be in upper case. |
   * | client.street_address | street address of the receiver. |
   * | client.zip_code | zip code of the receiver. |
   * | client.phone | Phone number of the receiver. Country code is mandatory.  <br>Example:
   * +234999999999 |
   * | payoutMethod | Payout-MOBILEMONEY |
   * | Amount | Amount in currency upto 2 decimal points |
   * | Currency | Note this is currency account for which you have balance. |
   * | success_redirect | URL to send the user if the payout is successful |
   * | pending_redirect | URL to send the user if the payout is in progress |
   * | failure_redirect | URL to send the user if the payout is unsuccessful. |
   * | Purpose | Purpose of Payout |
   *
   * ### Required parameters in the request body for this use case:
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | beneficiaryDetail.bankName | The Bank Name in which the customer want's to credit the money.  <br>
   * <br>Optional Field.  <br>If passed, only use the mentioned bank names below. Anything else might
   * lead to payout failures.  <br>If you don't want to pass, then you might need to open our
   * checkout_url in the response and user can select the Bank from the dropdown. |
   * | beneficiaryDetail.bankAccountName | Mandatory Field.  <br>Account Holder's Full Name.  <br>It is
   * advised to have atleast 2 words in the name. |
   * | beneficiaryDetail.bankAccountCurrency | Optional Field.  <br>The Currency in which the money will
   * be credited. Generally the same as the currency field mentioned above. |
   * | beneficiaryDetail.bankAccountNumber | Mandatory Field.  <br>Account Holder's Account Number.
   * <br>This is the account in which the money will be credited. |
   * | beneficiaryDetail.bankCountryCode | Optional Field.  <br>The country code in which the account
   * exist. Generally the same as the country field mentioned above. |
   *
   * @param body
   * @return Response from the API call
   */
  async createPayOut21(
    body: CreatePayOutRequest6,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success23>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, createPayOutRequest6Schema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, FailureError, 'Bad Request');
    req.authenticate([]);
    return req.callAsJson(success23Schema, requestOptions);
  }

  /**
   * This API tells you about all the details of a Payout, including its history
   *
   * ### Mandatory
   *
   * you need to pass the payoutId in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | paid | Transaction Successful |
   * | payout_in_process | payout is under Processing. |
   * | error | Transaction has Failed. |
   *
   * The webhook will only be triggered for paid transactions or errors.
   *
   * Paid: "success_callback": "[https://success.com](https://success.com)",
   *
   * Error: "failure_callback": “[https://failure.com”](https://failure.com”)
   *
   * @param payoutId
   * @return Response from the API call
   */
  async getStatus4(
    payoutId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<GetStatus>> {
    const req = this.createRequest('GET');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ payoutId: [payoutId, string()] });
    req.appendTemplatePath`/api/v1/getpayout/${mapped.payoutId}`;
    req.authenticate([]);
    return req.callAsJson(getStatusSchema, requestOptions);
  }
}
