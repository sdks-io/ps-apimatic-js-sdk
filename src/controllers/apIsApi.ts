/**
 * Paysecure API Documentation - LiveLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, FileWrapper, RequestOptions } from '../core.js';
import {
  CaptureRequest,
  captureRequestSchema,
} from '../models/captureRequest.js';
import {
  CheckWhiteListDoesnTExists,
  checkWhiteListDoesnTExistsSchema,
} from '../models/checkWhiteListDoesnTExists.js';
import {
  CheckWhiteListRequest,
  checkWhiteListRequestSchema,
} from '../models/checkWhiteListRequest.js';
import {
  CreateCustomerRequest1,
  createCustomerRequest1Schema,
} from '../models/createCustomerRequest1.js';
import {
  CreatePurchaseCitRequest,
  createPurchaseCitRequestSchema,
} from '../models/createPurchaseCitRequest.js';
import {
  CreatePurchaseMitRequest,
  createPurchaseMitRequestSchema,
} from '../models/createPurchaseMitRequest.js';
import {
  CreateSessionCitRequest,
  createSessionCitRequestSchema,
} from '../models/createSessionCitRequest.js';
import {
  OpenBankingEuUk,
  openBankingEuUkSchema,
} from '../models/openBankingEuUk.js';
import { PauseMandate, pauseMandateSchema } from '../models/pauseMandate.js';
import {
  PauseMandateRequest,
  pauseMandateRequestSchema,
} from '../models/pauseMandateRequest.js';
import {
  PaymentMethodSuccess,
  paymentMethodSuccessSchema,
} from '../models/paymentMethodSuccess.js';
import { PayOutRequest, payOutRequestSchema } from '../models/payOutRequest.js';
import {
  PayOutRequest3,
  payOutRequest3Schema,
} from '../models/payOutRequest3.js';
import { Purchase73, purchase73Schema } from '../models/purchase73.js';
import {
  PurchaseRequest,
  purchaseRequestSchema,
} from '../models/purchaseRequest.js';
import {
  PurchaseRequest2,
  purchaseRequest2Schema,
} from '../models/purchaseRequest2.js';
import {
  PurchaseRequest3,
  purchaseRequest3Schema,
} from '../models/purchaseRequest3.js';
import {
  PurchaseRequest4,
  purchaseRequest4Schema,
} from '../models/purchaseRequest4.js';
import {
  PurchasesDecryptedFlowRequest,
  purchasesDecryptedFlowRequestSchema,
} from '../models/purchasesDecryptedFlowRequest.js';
import {
  PurchasesDecryptedFlowRequest1,
  purchasesDecryptedFlowRequest1Schema,
} from '../models/purchasesDecryptedFlowRequest1.js';
import {
  PurchasesEncryptedFlowRequest,
  purchasesEncryptedFlowRequestSchema,
} from '../models/purchasesEncryptedFlowRequest.js';
import {
  PurchasesEncryptedFlowRequest1,
  purchasesEncryptedFlowRequest1Schema,
} from '../models/purchasesEncryptedFlowRequest1.js';
import {
  PurchasesError,
  purchasesErrorSchema,
} from '../models/purchasesError.js';
import {
  PurchasesRequest6,
  purchasesRequest6Schema,
} from '../models/purchasesRequest6.js';
import {
  PurchasesSuccess,
  purchasesSuccessSchema,
} from '../models/purchasesSuccess.js';
import {
  PurchaseSuccess4,
  purchaseSuccess4Schema,
} from '../models/purchaseSuccess4.js';
import { RefundSuccess, refundSuccessSchema } from '../models/refundSuccess.js';
import {
  RefundSuccess1,
  refundSuccess1Schema,
} from '../models/refundSuccess1.js';
import {
  RevokeMandateRequest,
  revokeMandateRequestSchema,
} from '../models/revokeMandateRequest.js';
import { S2SApiRequest, s2SApiRequestSchema } from '../models/s2SApiRequest.js';
import {
  S2SPaymentRequest,
  s2SPaymentRequestSchema,
} from '../models/s2SPaymentRequest.js';
import { S2Srequest, s2SrequestSchema } from '../models/s2Srequest.js';
import { Success, successSchema } from '../models/success.js';
import { Success1, success1Schema } from '../models/success1.js';
import { Success14, success14Schema } from '../models/success14.js';
import { Success19, success19Schema } from '../models/success19.js';
import { Success2, success2Schema } from '../models/success2.js';
import { Success32, success32Schema } from '../models/success32.js';
import { Success38, success38Schema } from '../models/success38.js';
import { Success39, success39Schema } from '../models/success39.js';
import { Success40, success40Schema } from '../models/success40.js';
import { Success41, success41Schema } from '../models/success41.js';
import { Success46, success46Schema } from '../models/success46.js';
import { Success47, success47Schema } from '../models/success47.js';
import { Success49, success49Schema } from '../models/success49.js';
import {
  WhitelistUploadSuccess,
  whitelistUploadSuccessSchema,
} from '../models/whitelistUploadSuccess.js';
import { array, boolean, string } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { ApiError } from '@apimatic/core';
import { CancelError } from '../errors/cancelError.js';
import { CaptureError } from '../errors/captureError.js';
import { Error1Error } from '../errors/error1Error.js';
import { FailureError } from '../errors/failureError.js';
import { InvalidSessionError } from '../errors/invalidSessionError.js';
import { PaymentError } from '../errors/paymentError.js';
import { PaymentMethodsError } from '../errors/paymentMethodsError.js';
import { RefundError } from '../errors/refundError.js';
import { Success33Error } from '../errors/success33Error.js';
import { Success36Error } from '../errors/success36Error.js';
import { WhitelistUploadFailureError } from '../errors/whitelistUploadFailureError.js';

export class ApIsApi extends BaseApi {
  /**
   * To Initiate a payment, the very first call to make is `/purchases` with the required data in the
   * request body.
   *
   * To generate a Purchase, you are required to provide the `Brand ID` (in the request body) and `API
   * key` (in the header) Both can be located in the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a purchase
   * request.
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.email | The customer's email. |
   * | client.city | The customer's city. |
   * | client.country | ISO-3166 Country Code. Must be upper case. Example “SG” (Alpha2) |
   * | client.stateCode | Example “AL”, “XZ”. Must be in upper case. |
   * | Client.street_address | The customer's address. |
   * | client.zip_code | The customer's ZIP or postal code. If country=US, zip format must be NNNNN or
   * NNNNN-NNNN. |
   * | purchase.currency | ISO 4217 code for currency you want to send the transaction in.  <br>
   * <br>Please note, the currency has to be enabled by the account manager for your account. |
   * | purchase.products | An object which contains the list of products which the customer is buying. |
   * | purchase.products.name | The name of the product. |
   * | purchase.products.price | Price in decimal format.  <br>example 1:  <br>EUR 5 , should be sent as
   * 5.00  <br>  <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.37 |
   * | brand_id | Obtain from Dashboard section of your merchant account login. |
   * | success_redirect | URL to send the user if the transactions is successful. |
   * | pending_redirect | URL to send the user if the transactions is in pending. |
   * | failure_redirect | URL to send the user if the transactions is unsuccessful. |
   *
   * ## Essential Optional Parameters
   *
   * ### merchantRef
   *
   * The `merchantRef` parameter, although not mandatory, is strongly advised for merchants to specify.
   * Its inclusion offers two significant advantages for the merchant:
   *
   * 1. **Prevention of Duplicate Requests:** When a `merchantRef` is utilized, it acts as a unique
   * identifier. This means that if a second purchase request is made using the same `merchantRef`
   * (reference number), the system will recognize it and prevent duplicate transactions from occurring.
   * This prevents unintended or duplicate purchases.
   *
   * 2. **Facilitation of Transaction Retrieval:** In situations where the response from the initial
   * purchase request times out or the 'purchaseId' isn't received, having the `merchantRef` allows the
   * merchant to retrieve detailed transaction information. This ensures they can track and access the
   * specific purchase details related to that reference, even if the immediate response was not received.
   *
   *
   * However, if the merchant does not specify the 'merchantRef' parameter, the platform will
   * automatically assign and use an internal 'purchaseId' as a reference for that transaction.
   *
   * Upon successul create on a Purchase you'd get a `"purchaseId"` . Use this ID for the next step in
   * Server to server call by calling `p/{purchaseId}/?s2s=true .`
   *
   * ### paymentMethod
   *
   * The `paymentMethod` parameter identifies which payment solution merchant wants to use to perform a
   * transaction. If parameter is provided, than Paysecure will perform direct payment with the selected
   * payment method instead of loading Paysecure URL.
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * ### Possible Error Messages
   *
   * | **Error Messages** |
   * | --- |
   * | Allowed Limit for this card for particular time period has been consumed |
   * | some Mandatory Parameter are missing |
   * | Invalid format of Date_of_Birth \[allowed format: yyyy-mm-dd\] |
   * | Invalid Email Format |
   * | Please submit Valid Alpha2 Country Code Ex:(AF,IN) in \\"country\\" parameter |
   * | Please pass Valid State Code |
   * | Please pass valid street address in \\"street_address\\" parameter |
   * | Please pass valid city name in \\"city\\" parameter |
   * | Please pass valid postal Code name in \\"zip_code\\" parameter |
   * | You are not Allowed for Live Transaction |
   * | Merchant Limit is not set |
   * | Minimum amount is not set for this merchant |
   * | Brand not found ! |
   * | Your charges setting is incomplete .Plese Contact to Administrator. |
   * | Only {{currencyCode}} currency is allowed. |
   * | Transaction amount must be equal or greater to minimum trans amount |
   * | Invalid_Parameter  <br>You'd usually get it when one or more mandatory parameters are not present
   * in the request. |
   *
   * @param body
   * @return Response from the API call
   */
  async purchase(
    body: PurchaseRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ body: [body, purchaseRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(successSchema, requestOptions);
  }

  /**
   * This API would be the 2nd in sequence to call if the call to `/purchases/` was successful in step 1.
   * and you want to do the transaction via Server-to-Server mode.
   *
   * The request body would contain the details of the card that's to be transacted upon.
   *
   * ### **Mandatory parameters in request body:**
   *
   * | Parameter | **Notes** |
   * | --- | --- |
   * | remote_ip | IP address of the customer’s device used for transaction. Helps in fraud checks and
   * risk scoring. Ex- 157.38.242.7 |
   * | remember_card | "on" or "off" if you want us to store the card details on our end. |
   * | user_agent | Full browser and operating system details captured from HTTP header. EX- Chrome/5.0
   * (X11; Linux x86_64) |
   * | accept_header | Browser’s accepted content types (from HTTP header). Used for validating request
   * origin and device info. EX- text/html |
   *
   * **Note:**
   * If you want to use token_reference for doing a card transaction, please have a look at Network
   * Tokenisation Section.
   *
   * ### **Essential parameters in request body:**
   *
   * | Parameter | **Notes** |
   * | --- | --- |
   * | cardholder_name | Name of the cardholder |
   * | card_number | The card numbers Must be 10-20 characters. |
   * | expires | must be greater than the current month/year. MM/YY format. |
   * | cvc | 3 or 4 digit |
   *
   *
   * **Other optional parameters in request body:**
   *
   * | Parameter | **Notes** |
   * | --- | --- |
   * | language | Preferred language of the user’s browser . EX - en-US |
   * | java_enabled | Indicates if Java is enabled in the browser. Helps PSPs in device profiling. Ex-
   * true/false |
   * | javascript_enabled | Indicates if JavaScript is enabled in the client browser. Used for 3DS or
   * risk-based checks. Ex- true |
   * | color_depth | Bit depth of the display screen. Ex- 24 |
   * | utc_offset | Difference in minutes between local time and UTC. Used for location and timezone
   * checks. Ex- 330 |
   * | screen_width | Width of the device screen in pixels. Used in device profiling. Ex- 1920 |
   * | screen_height | Height of the device screen in pixels. Ex- 1080 |
   *
   * ### Successful Response
   *
   * If all the details are correct you'll get a 202 response with staus as Pending
   *
   * ``` json
   * {
   * "status": "pending",
   * "callback_url": "https://paysecure.net/payment/63bd0bf80fb42a076e8a4dd1/",
   * "method": "GET"
   * }
   *
   * ```
   *
   * If the response code is 202, after receiving the response body, direct the customer to the
   * callback_url provided in the response.
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * Please see the Status Code section for further details.
   *
   * | **Error Messages** |
   * | --- |
   * | Client Ip could not be matched with Merchant Ip |
   * | Invalid Card Information |
   * | Card is Blocked |
   * | Different Type of key used to create purchase and payment |
   * | You charges setting is incomplete .Plese Contact to adminstrartor. |
   * | Allowed Attempt for this Transaction has been consumed |
   * | Invalid Card Expiry(Valid Format:MM/YY) must be greator than current month/year |
   * | Customer profile is Blocked |
   * | Customer/Card not allowed for transaction |
   *
   * @param s2S
   * @param body
   * @return Response from the API call
   */
  async s2S(
    s2S: boolean,
    body: S2Srequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success1>> {
    const req = this.createRequest('POST', '/api/v1/p/purchaseid');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      s2S: [s2S, boolean()],
      body: [body, s2SrequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.query('s2s', mapped.s2S);
    req.json(mapped.body);
    req.throwOn(401, FailureError, 'Unauthorized');
    req.authenticate([]);
    return req.callAsJson(success1Schema, requestOptions);
  }

  /**
   * This is the callback URL that is in the response object of the Server-to-Server Call
   *
   * When this URL is called from the end user's browser, the user would be redirected to the
   * `success_redirect` or `failure_redirect` URL given by the merchant at the time of create purchase
   *
   * When this URL is called from the end user's browser, if card is 3DS enrolled, the user will be
   * redirect to a verification screen or challenge by the issuing bank and upon successful user
   * verification, the user would be redictected to the success URL given in `/purchases API` .
   *
   * If for some reason the transactions is unsuccessful, the user will be redirect to failure URL given
   * in `/purchases API` API .
   *
   * @param accept
   * @return Response from the API call
   */
  async paymentToBeInitiatedFromClientBrowser(
    accept: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('GET', '/api/v1/payment/purchaseid');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ accept: [accept, string()] });
    req.header('Accept', mapped.accept);
    req.throwOn(400, PaymentError, 'Bad Request');
    req.authenticate([]);
    return req.call(requestOptions);
  }

  /**
   * This API tells you about all the details of a purchase, including its history
   *
   * ### Mandatory
   *
   * you need to pass the `PurchaseID` in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | created | When Purchase order is created. |
   * | paid | Transaction Successful |
   * | payment_in_process | Payment is under Processing |
   * | chargeback | Payment is chargeback state |
   * | refund_in_process | Refund is under Processing. |
   * | expired | Purchase has Expired. |
   * | overdue | Purchase is Overdued. |
   * | cancelled | Purchase is cancelled. |
   * | error | Transaction has Failed. |
   * | refunded | Payment is refunded |
   *
   * ### Response Examples
   *
   * In the response examples you can see instances of both `Paid` and `Error` Status purchases.
   *
   * ### Error Details
   *
   * If there are any issues on the Purchase you can get more details from the `transaction_data.attempts.
   * error` attribute of the response object
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * Please see the Status Code section for further details.
   *
   * @return Response from the API call
   */
  async getStatus(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchasesError>> {
    const req = this.createRequest('GET', '/api/v1/purchases/purchaseid');
    req.baseUrl('Server 3');
    req.header('Content-Type', 'application/json');
    req.authenticate([]);
    return req.callAsJson(purchasesErrorSchema, requestOptions);
  }

  /**
   * This API is used to initate refund (Full Refund) of the purchases that have status as `PAID` can be
   * initiated for refund
   *
   * Once the refund has been initiated, the status of the purchase would become `REFUND_IN_PROCESS` and
   * once the refund has been approved by the bank, the status woud become `REFUNDED`
   *
   * Purchase ID is **mandatory** for this API.
   *
   * Currently we don't support partial refunds.
   *
   * ### Response Examples
   *
   * In the response examples you can see instances of both `successful` and `unsuccessful` in refund of
   * purchases.
   *
   * @return Response from the API call
   */
  async refund(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RefundSuccess>> {
    const req = this.createRequest(
      'GET',
      '/api/v1/purchases/purchaseid/refund'
    );
    req.baseUrl('Server 3');
    req.header('Content-Type', 'application/json');
    req.throwOn(400, RefundError, 'Bad Request');
    req.authenticate([]);
    return req.callAsJson(refundSuccessSchema, requestOptions);
  }

  /**
   * This API gives back the list of Payment methods available for a brand.
   *
   * @param brandId
   * @return Response from the API call
   */
  async paymentMethods(
    brandId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PaymentMethodSuccess>> {
    const req = this.createRequest('GET', '/api/v1/payment_methods');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ brandId: [brandId, string()] });
    req.header('Content-Type', 'application/json');
    req.query('brand_id', mapped.brandId);
    req.throwOn(401, PaymentMethodsError, 'Unauthorized');
    req.authenticate([]);
    return req.callAsJson(paymentMethodSuccessSchema, requestOptions);
  }

  /**
   * Cancels an in-progress or completed purchase transaction, preventing further processing or reversing
   * the authorization hold. Use this endpoint to void a transaction that has not yet been settled to the
   * merchant's account.
   *
   * **When to use:**
   *
   * - **Post-creation cancellation:** Cancel a purchase immediately after it has been created via the
   * Purchase API, before the customer completes the payment flow.
   *
   * - **Pre-redirect cancellation:** Cancel a transaction after completing the server-to-server (S2S)
   * call but before redirecting the cardholder to the `callbackURL`. This is useful when your backend
   * validation logic determines the transaction should not proceed (e.g., inventory check failure, fraud
   * flag, duplicate order detection).
   *
   *
   * Purchase ID is **mandatory** for this API.
   *
   * @return Response from the API call
   */
  async cancel(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success2>> {
    const req = this.createRequest(
      'GET',
      '/api/v1/purchases/64bff556f63e36669499e9d3/cancel'
    );
    req.baseUrl('Server 3');
    req.header('Content-Type', 'application/json');
    req.throwOn(400, Error1Error, 'Bad Request');
    req.authenticate([]);
    return req.callAsJson(success2Schema, requestOptions);
  }

  /**
   * ### Overview
   *
   * Paysecure supports the concept of whitelisted cards. The merchant can send PaySecure a list of cards
   * that are to be whitelisted. Once whitelisted, only whitelisted cards are allowed to be transacted
   * for that merchant.
   *
   * The merchant can check the individual card status with this API by giving the email and card numbers
   * of the customers.
   *
   * ### Mandatory Parameters (in body)
   *
   * | **Parameters** |
   * | --- |
   * | email |
   * | card_num |
   *
   * ### Other parameters:
   *
   * | **Parameters** |
   * | --- |
   * | signup_date |
   * | deposit_date |
   * | number_of_deposits |
   *
   * ### Response Examples
   *
   * In the response examples, you can observe both instances of verifying whether a card exists in a
   * merchant's whitelist, one for an existing card and another for a non-existing card.
   *
   * @param accept
   * @param body
   * @return Response from the API call
   */
  async checkWhiteList(
    accept: string,
    body: CheckWhiteListRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CheckWhiteListDoesnTExists>> {
    const req = this.createRequest('POST', '/api/v1/checkWhiteList');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      accept: [accept, string()],
      body: [body, checkWhiteListRequestSchema],
    });
    req.header('Accept', mapped.accept);
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(checkWhiteListDoesnTExistsSchema, requestOptions);
  }

  /**
   * ### Overview
   *
   * The merchant can upload a list of whitelist cards and email ids through this API. All cards
   * successfully uploaded will be considered whitelisted and allowed for that merchant.
   *
   * ### Mandatory parameters in the request body:
   *
   * | Parameter | Notes |
   * | --- | --- |
   * | csvFile | Upload a CSV file |
   *
   * **Please note:**
   *
   * 1. CSV file should contain the following headers (both mandatory)
   *
   * 1. card_number
   *
   * 2. email_id
   *
   * 2. CSV file should not be empty
   *
   *
   * ``` java
   * public class FileUploader {
   * public static void uploadFile(String apiUrl, String filePath, String authorizationHeader) {
   * try {
   * File file = new File(filePath);
   * OkHttpClient client = new OkHttpClient();
   * RequestBody requestBody = new MultipartBody.Builder().setType(MultipartBody.FORM)
   * .addFormDataPart("csvFile", file.getName(), RequestBody.create(MediaType.
   * parse("text/csv"), file))
   * .build();
   * Request request = new Request.Builder().url(apiUrl).post(requestBody)
   * .addHeader("Authorization", authorizationHeader).build();
   * Response response = client.newCall(request).execute();
   * if (response.code() == 202) {
   * System.out.println("File uploaded successfully!");
   * System.out.println("Response: " + response.body().string());
   * } else {
   * System.out.println("File upload failed.");
   * System.out.println("Status: " + response.code());
   * System.out.println("Error: " + response.body().string());
   * }
   * } catch (IOException e) {
   * e.printStackTrace();
   * }
   * }
   * public static void main(String[] args) {
   * String apiUrl = "https://{{base_url}}/api/v1/whitelist/upload";
   * String filePath = "whitelist.csv.file.path";
   * String authorizationHeader = "Bearer YOUR API KEY";
   * uploadFile(apiUrl, filePath, authorizationHeader);
   * }
   * }
   *
   * ```
   *
   * @param csvFile
   * @return Response from the API call
   */
  async whitelistUpload(
    csvFile: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<WhitelistUploadSuccess>> {
    const req = this.createRequest('POST', '/api/v1/whitelist/upload');
    req.baseUrl('Server 3');
    req.header(
      'Content-Type',
      'multipart/form-data; boundary=<calculated when request is sent>'
    );
    req.formData({ csvFile: csvFile });
    req.throwOn(401, WhitelistUploadFailureError, 'Unauthorized');
    req.authenticate([]);
    return req.callAsJson(whitelistUploadSuccessSchema, requestOptions);
  }

  /**
   * ## Overview
   *
   * The **Original Credit Transaction (OCT)**, commonly referred to as **Push to Card**, enables
   * merchants to facilitate near-instantaneous fund transfers directly to a customer’s eligible Visa or
   * Mastercard debit or credit card. Unlike traditional refund processes that can only return funds from
   * a previous purchase, OCT allows for independent disbursements of funds to a cardholder's account.
   *
   * This feature is designed for high-velocity industries that require frictionless, real-time payouts,
   * such as:
   *
   * - **Gaming & Betting:** Real-time withdrawal of player winnings.
   *
   * - **Marketplaces:** Payouts to sellers or service providers.
   *
   * - **Corporate Disbursements:** Instant insurance claims, rebates, or compensation payments.
   *
   * - **Merchant Settlements:** Faster access to processed funds for sub-merchants.
   *
   *
   * By utilizing the Paysecure `/payout` API with the `PAYOUT-CARDS` method, you benefit from a unified
   * orchestration layer that handles the complexities of card network routing, ensuring high success
   * rates and global reach while maintaining the same security standards as your standard payment
   * integrations.
   *
   * ---
   *
   * ## Integration Steps
   *
   * 1. **Enable OCT:** Ensure "Push to Card" permissions are enabled for your merchant account in the
   * Paysecure Merchant Portal.
   *
   * 2. **Collect Card Details:** Collect the recipient's card details securely, or use Paysecure's
   * Cashier to collect the card information on your behalf.
   *
   * 3. **Execute Payout:** Call the `/payout` endpoint with the OCT-specific payout method.
   *
   * 4. **Handle Webhooks:** Listen for the `paid` or `failure` events to update your internal records.
   *
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Required** | **Notes** |
   * | --- | --- | --- |
   * | client.email | Yes | The customer's email. |
   * | client.phone | Yes | The customer's phone number along with the country code.  <br>Example: +91
   * 9634088651 |
   * | client.city | Yes | The customer's city. |
   * | client.country | Yes | ISO-3166 Country Code. Must be upper case. Example “SG” (Alpha2) |
   * | client.stateCode | Optional | Example “AL”, “XZ”. Must be in upper case. |
   * | Client.street_address | Optional | The customer's address. |
   * | client.zip_code | Optional | The customer's ZIP or postal code. If country=US, zip format must be
   * NNNNN or NNNNN-NNNN. |
   * | currency | Yes | ISO 4217 code for currency you want to send the transaction in.  <br>  <br>Please
   * note, the currency has to be enabled by the account manager for your account. |
   * | amount | Yes | amount in decimal format.  <br>example 1:  <br>EUR 5 , should be sent as 5.00  <br>
   * <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.37 |
   * | payoutMethod | Optional | example: PAYOUT-CARDS |
   * | success_redirect | Yes | URL to send the user if the transactions is successful. |
   * | pending_redirect | Yes | URL to send the user if the transactions is in pending. |
   * | failure_redirect | Yes | URL to send the user if the transactions is unsuccessful. |
   * | Purpose | Optional | The purpose of doing a payout. |
   * | extraParam.cardNumber | Yes | The recipient's 16-digit card number. |
   * | extraParam.expiryYear | Yes | YYYY format (e.g., 2028). |
   * | extraParam.expiryMonth | Yes | MM format (e.g., 05). |
   * | extraParam.cardHolderName | Yes | The receiver's Full Name. Preferred a 2 word name with each word
   * atleast 3 characters long. |
   *
   * **Note:**
   *
   * If the merchant is not PCI DSS compliant then they will need to use our **cashier** and asking the
   * user to enter the card details there. Check the cashier section for more details. If the merchant is
   * PCI DSS compliant then they can use the payout API and send the card details as shown above.
   *
   * If the merchant wants to use **token_reference** for doing a payout, then please have a look at the
   * Network Tokenisation Section.
   *
   * @param accept
   * @param body
   * @return Response from the API call
   */
  async payOut(
    accept: string,
    body: PayOutRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<string>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      accept: [accept, string()],
      body: [body, payOutRequestSchema],
    });
    req.header('Accept', mapped.accept);
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsText(requestOptions);
  }

  /**
   * To Initiate a payment, the very first call to make is `/purchases` with the required data in the
   * request body.
   *
   * To generate a Purchase, you are required to provide the `Brand ID` (in the request body) and `API
   * key` (in the header) Both can be located in the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a purchase
   * request.
   *
   * ### **Parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.full_name | Conditional.  <br>But Recommended to pass the complete name of the client.
   * Atleast 2 words recommended. |
   * | client.email | Conditional.  <br>But Recommended to pass a correct email id. |
   * | paymentMethod | Conditional.  <br>Mandatory if you are not using Paysecure Cashier. Else Non
   * Mandatory.  <br>Value: GOOGLEPAY-REDIRECT |
   * | purchase.currency | Mandatory.  <br>ISO 4217 code for currency you want to send the transaction in.
   * This should be 'BRL' for PIX. |
   * | purchase.products.name | Mandatory. |
   * | purchase.products.price | Mandatory.  <br>Price in decimal format upto 2 decimals.  <br>example 1:
   * <br>EUR 5 , should be sent as 5.00  <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.
   * 37 |
   * | brand_id | Mandatory.  <br>Obtain from Dashboard section of your merchant account login. |
   * | success_redirect | Mandatory.  <br>URL to send the user if the transactions is successful. |
   * | pending_redirect | Mandatory.  <br>URL to send the user if the transactions is in progress state.
   * |
   * | failure_redirect | Mandatory  <br>URL to send the user if the transactions has failed. |
   *
   * ## Optional Parameters
   *
   * ### expireInMin
   *
   * This parameter, when passed in the PayIn request, sets the time window within which a customer
   * should make the Interac payment through their bank's app. If a customer doesn't complete the payment
   * within the configured time window, the status of the payment will be marked as 'EXPIRED' in
   * Paysecure.
   *
   * However, in certain cases, a payment made outside of this window could still be successful i.e.
   * customer's account may be debited which would result in inconsistencies among Paysecure's,
   * merchant's and customer's status of the transaction. To avoid such cases it is advised to set the
   * expiry time limit appropriately and advise the customers to make payment within this window. However,
   * if there is an inconsistency between the status at Paysecure and at customer's Bank, the merchant
   * and Paysecure will reconcile the amounts during the settlement.
   *
   * ### merchantRef
   *
   * The `merchantRef` parameter, although not mandatory, is strongly advised for merchants to specify.
   * Its inclusion offers two significant advantages for the merchant:
   *
   * 1. **Prevention of Duplicate Requests:** When a `merchantRef` is utilized, it acts as a unique
   * identifier. This means that if a second purchase request is made using the same `merchantRef`
   * (reference number), the system will recognize it and prevent duplicate transactions from occurring.
   * This prevents unintended or duplicate purchases.
   *
   * 2. **Facilitation of Transaction Retrieval:** In situations where the response from the initial
   * purchase request times out or the 'purchaseId' isn't received, having the `merchantRef` allows the
   * merchant to retrieve detailed transaction information. This ensures they can track and access the
   * specific purchase details related to that reference, even if the immediate response was not received.
   *
   *
   * However, if the merchant does not specify the 'merchantRef' parameter, the platform will
   * automatically assign and use an internal 'purchaseId' as a reference for that transaction.
   *
   * ### paymentMethod
   *
   * The `paymentMethod` parameter identifies which payment solution merchant wants to use to perform a
   * transaction. If parameter is provided, than Paysecure will perform direct payment with the selected
   * payment method instead of loading Paysecure URL.
   *
   * @param body
   * @return Response from the API call
   */
  async purchases(
    body: PurchasesRequest6,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<string>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ body: [body, purchasesRequest6Schema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsText(requestOptions);
  }

  /**
   * **OVERVIEW**
   *
   * The `createSession` API is designed to initiate a payment session for a customer after their profile
   * has been created via the **createCustomer** API. The session is automatically generated at the
   * backend on the merchant’s checkout URL. This session allows the customer to continue with their
   * transaction without having to manually interact with the session URL, and ensures the session
   * remains active until the payment is completed or the session expires.
   *
   * This API improves the user experience by reducing transaction failures and enhancing the overall
   * payment process, thus reducing transaction churn rates.
   *
   * ### Mandatory Parameters
   *
   * | Parameter | Type | Description | Example |
   * | --- | --- | --- | --- |
   * | customerId | String | The unique ID of the customer (generated from createCustomer). |
   * 6731a609b6bb5a43ad66c4a6 |
   *
   * ### **Usage Flow**
   *
   * 1. **Customer completes the** **`createCustomer`** **API**: Once the customer is created via the
   * `createCustomer` API, the merchant proceeds to call the `createSession` API to generate a session.
   *
   * 2. **Merchant sends** **`createSession`** **request**: The merchant sends a `POST` request to the
   * `createSession` API with the customer’s `customerId` and the merchant’s `brandId`.
   *
   * 3. **Session created automatically on the checkout URL**: PaySecure creates the session and
   * associates it with the merchant’s checkout page. This session remains active on the backend,
   * allowing the customer to continue the payment process without interruption.
   *
   * 4. **Customer proceeds to checkout**: The customer is redirected to the checkout page as part of the
   * merchant's payment flow, where the session is automatically validated. The session remains active
   * for a set period, allowing the customer to complete the payment without needing to manually interact
   * with the session URL.
   *
   * 5. **Session Expiry**: The session expires after the set expiration time (`expiryOn`), ensuring the
   * payment process is completed within the defined window. Default is 15 mins from the creation time.
   *
   *
   * ---
   *
   * ### **Benefits**
   *
   * - **Seamless Checkout**: The session URL is automatically generated and active at the backend,
   * ensuring a smoother and uninterrupted customer experience.
   *
   * - **Reduced Transaction Failures**: By ensuring the session remains active during the payment
   * process, the chances of transaction failures due to session timeouts are minimized.
   *
   * - **Improved User Experience**: The customer does not need to click on a session URL or navigate
   * away from the page, leading to faster and more secure payment processing.
   *
   * @return Response from the API call
   */
  async session(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success14>> {
    const req = this.createRequest('POST', '/api/v1/createSession');
    req.baseUrl('Server 3');
    req.throwOn(400, InvalidSessionError, 'Bad Request');
    req.authenticate(false);
    return req.callAsJson(success14Schema, requestOptions);
  }

  /**
   * This API tells you about all the details of a purchase (PayIn), including its history
   *
   * ### Mandatory
   *
   * you need to pass the `PurchaseID` in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | paid | Transaction Successful |
   * | payment_in_process | Payment is under Processing |
   * | expired | Purchase has Expired. |
   * | error | Transaction has Failed. |
   *
   * ### Response Examples
   *
   * In the response examples you can see instances of both `Paid` and `Error` Status purchases.
   *
   * ### Error Details
   *
   * If there are any issues on the Purchase you can get more details from the `transaction_data.attempts.
   * error` attribute of the response object
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * Please see the Status Code section for further details.
   *
   * @param purchaseId
   * @return Response from the API call
   */
  async getStatus5(
    purchaseId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<string>> {
    const req = this.createRequest('GET');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ purchaseId: [purchaseId, string()] });
    req.header('Content-Type', 'application/json');
    req.appendTemplatePath`/api/v1/purchases/${mapped.purchaseId}`;
    req.authenticate([]);
    return req.callAsText(requestOptions);
  }

  /**
   * To Initiate a payment, the very first call to make is `/purchases` with the required data in the
   * request body.
   *
   * To generate a Purchase, you are required to provide the `Brand ID` (in the request body) and `API
   * key` (in the header) Both can be located in the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a purchase
   * request.
   *
   * ### **Parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.full_name | Conditional.  <br>But Recommended to pass the complete name of the client.
   * Atleast 2 words recommended. |
   * | client.email | Conditional.  <br>But Recommended to pass a correct email id. |
   * | paymentMethod | Conditional.  <br>Mandatory if you are not using Paysecure Cashier. Else Non
   * Mandatory.  <br>Value: APPLEPAY-REDIRECT |
   * | purchase.currency | Mandatory.  <br>ISO 4217 code for currency you want to send the transaction in.
   * This should be 'BRL' for PIX. |
   * | purchase.products.name | Mandatory. |
   * | purchase.products.price | Mandatory.  <br>Price in decimal format upto 2 decimals.  <br>example 1:
   * <br>EUR 5 , should be sent as 5.00  <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.
   * 37 |
   * | brand_id | Mandatory.  <br>Obtain from Dashboard section of your merchant account login. |
   * | success_redirect | Mandatory.  <br>URL to send the user if the transactions is successful. |
   * | pending_redirect | Mandatory.  <br>URL to send the user if the transactions is in progress state.
   * |
   * | failure_redirect | Mandatory  <br>URL to send the user if the transactions has failed. |
   *
   * ## Optional Parameters
   *
   * ### expireInMin
   *
   * This parameter, when passed in the PayIn request, sets the time window within which a customer
   * should make the Interac payment through their bank's app. If a customer doesn't complete the payment
   * within the configured time window, the status of the payment will be marked as 'EXPIRED' in
   * Paysecure.
   *
   * However, in certain cases, a payment made outside of this window could still be successful i.e.
   * customer's account may be debited which would result in inconsistencies among Paysecure's,
   * merchant's and customer's status of the transaction. To avoid such cases it is advised to set the
   * expiry time limit appropriately and advise the customers to make payment within this window. However,
   * if there is an inconsistency between the status at Paysecure and at customer's Bank, the merchant
   * and Paysecure will reconcile the amounts during the settlement.
   *
   * ### merchantRef
   *
   * The `merchantRef` parameter, although not mandatory, is strongly advised for merchants to specify.
   * Its inclusion offers two significant advantages for the merchant:
   *
   * 1. **Prevention of Duplicate Requests:** When a `merchantRef` is utilized, it acts as a unique
   * identifier. This means that if a second purchase request is made using the same `merchantRef`
   * (reference number), the system will recognize it and prevent duplicate transactions from occurring.
   * This prevents unintended or duplicate purchases.
   *
   * 2. **Facilitation of Transaction Retrieval:** In situations where the response from the initial
   * purchase request times out or the 'purchaseId' isn't received, having the `merchantRef` allows the
   * merchant to retrieve detailed transaction information. This ensures they can track and access the
   * specific purchase details related to that reference, even if the immediate response was not received.
   *
   *
   * However, if the merchant does not specify the 'merchantRef' parameter, the platform will
   * automatically assign and use an internal 'purchaseId' as a reference for that transaction.
   *
   * ### paymentMethod
   *
   * The `paymentMethod` parameter identifies which payment solution merchant wants to use to perform a
   * transaction. If parameter is provided, than Paysecure will perform direct payment with the selected
   * payment method instead of loading Paysecure URL.
   *
   * @param body
   * @return Response from the API call
   */
  async purchases1(
    body: PurchasesRequest6,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<string>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ body: [body, purchasesRequest6Schema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsText(requestOptions);
  }

  /**
   * **OVERVIEW**
   *
   * The `createSession` API is designed to initiate a payment session for a customer after their profile
   * has been created via the **createCustomer** API. The session is automatically generated at the
   * backend on the merchant’s checkout URL. This session allows the customer to continue with their
   * transaction without having to manually interact with the session URL, and ensures the session
   * remains active until the payment is completed or the session expires.
   *
   * This API improves the user experience by reducing transaction failures and enhancing the overall
   * payment process, thus reducing transaction churn rates.
   *
   * ### Mandatory Parameters
   *
   * | Parameter | Type | Description | Example |
   * | --- | --- | --- | --- |
   * | customerId | String | The unique ID of the customer (generated from createCustomer). |
   * 6731a609b6bb5a43ad66c4a6 |
   *
   * ### **Usage Flow**
   *
   * 1. **Customer completes the** **`createCustomer`** **API**: Once the customer is created via the
   * `createCustomer` API, the merchant proceeds to call the `createSession` API to generate a session.
   *
   * 2. **Merchant sends** **`createSession`** **request**: The merchant sends a `POST` request to the
   * `createSession` API with the customer’s `customerId` and the merchant’s `brandId`.
   *
   * 3. **Session created automatically on the checkout URL**: PaySecure creates the session and
   * associates it with the merchant’s checkout page. This session remains active on the backend,
   * allowing the customer to continue the payment process without interruption.
   *
   * 4. **Customer proceeds to checkout**: The customer is redirected to the checkout page as part of the
   * merchant's payment flow, where the session is automatically validated. The session remains active
   * for a set period, allowing the customer to complete the payment without needing to manually interact
   * with the session URL.
   *
   * 5. **Session Expiry**: The session expires after the set expiration time (`expiryOn`), ensuring the
   * payment process is completed within the defined window. Default is 15 mins from the creation time.
   *
   *
   * ---
   *
   * ### **Benefits**
   *
   * - **Seamless Checkout**: The session URL is automatically generated and active at the backend,
   * ensuring a smoother and uninterrupted customer experience.
   *
   * - **Reduced Transaction Failures**: By ensuring the session remains active during the payment
   * process, the chances of transaction failures due to session timeouts are minimized.
   *
   * - **Improved User Experience**: The customer does not need to click on a session URL or navigate
   * away from the page, leading to faster and more secure payment processing.
   *
   * @return Response from the API call
   */
  async session1(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success14>> {
    const req = this.createRequest('POST', '/api/v1/createSession');
    req.baseUrl('Server 3');
    req.throwOn(400, InvalidSessionError, 'Bad Request');
    req.authenticate(false);
    return req.callAsJson(success14Schema, requestOptions);
  }

  /**
   * This API tells you about all the details of a purchase (PayIn), including its history
   *
   * ### Mandatory
   *
   * you need to pass the `PurchaseID` in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | paid | Transaction Successful |
   * | payment_in_process | Payment is under Processing |
   * | expired | Purchase has Expired. |
   * | error | Transaction has Failed. |
   *
   * ### Response Examples
   *
   * In the response examples you can see instances of both `Paid` and `Error` Status purchases.
   *
   * ### Error Details
   *
   * If there are any issues on the Purchase you can get more details from the `transaction_data.attempts.
   * error` attribute of the response object
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * Please see the Status Code section for further details.
   *
   * @param purchaseId
   * @return Response from the API call
   */
  async getStatus1(
    purchaseId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<string>> {
    const req = this.createRequest('GET');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ purchaseId: [purchaseId, string()] });
    req.header('Content-Type', 'application/json');
    req.appendTemplatePath`/api/v1/purchases/${mapped.purchaseId}`;
    req.authenticate([]);
    return req.callAsText(requestOptions);
  }

  /**
   * To process a Google Pay transaction, a Purchase must first be created using the /purchases API. The
   * Google Pay token must be included in the request under extraParam.googlepay_param.
   *
   * To generate a Purchase, you are required to provide the `Brand ID` (in the request body) and `API
   * key` (in the header) Both can be located in the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a purchase
   * request.
   *
   * ### **Parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.full_name | Conditional.  <br>But Recommended to pass the complete name of the client.
   * Atleast 2 words recommended. |
   * | client.email | Conditional.  <br>But Recommended to pass a correct email id. |
   * | paymentMethod | Conditional.  <br>Mandatory if you are not using Paysecure Cashier. Else Non
   * Mandatory.  <br>Value: GOOGLEPAY |
   * | purchase.currency | Mandatory.  <br>ISO 4217 code for currency you want to send the transaction in.
   * This should be 'BRL' for PIX. |
   * | purchase.products.name | Mandatory. |
   * | purchase.products.price | Mandatory.  <br>Price in decimal format upto 2 decimals.  <br>example 1:
   * <br>EUR 5 , should be sent as 5.00  <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.
   * 37 |
   * | brand_id | Mandatory.  <br>Obtain from Dashboard section of your merchant account login. |
   * | success_redirect | Mandatory.  <br>URL to send the user if the transactions is successful. |
   * | pending_redirect | Mandatory.  <br>URL to send the user if the transactions is in progress state.
   * |
   * | failure_redirect | Mandatory  <br>URL to send the user if the transactions has failed. |
   *
   * ## Optional Parameters
   *
   * ### expireInMin
   *
   * This parameter, when passed in the PayIn request, sets the time window within which a customer
   * should make the Interac payment through their bank's app. If a customer doesn't complete the payment
   * within the configured time window, the status of the payment will be marked as 'EXPIRED' in
   * Paysecure.
   *
   * However, in certain cases, a payment made outside of this window could still be successful i.e.
   * customer's account may be debited which would result in inconsistencies among Paysecure's,
   * merchant's and customer's status of the transaction. To avoid such cases it is advised to set the
   * expiry time limit appropriately and advise the customers to make payment within this window. However,
   * if there is an inconsistency between the status at Paysecure and at customer's Bank, the merchant
   * and Paysecure will reconcile the amounts during the settlement.
   *
   * ### merchantRef
   *
   * The `merchantRef` parameter, although not mandatory, is strongly advised for merchants to specify.
   * Its inclusion offers two significant advantages for the merchant:
   *
   * 1. **Prevention of Duplicate Requests:** When a `merchantRef` is utilized, it acts as a unique
   * identifier. This means that if a second purchase request is made using the same `merchantRef`
   * (reference number), the system will recognize it and prevent duplicate transactions from occurring.
   * This prevents unintended or duplicate purchases.
   *
   * 2. **Facilitation of Transaction Retrieval:** In situations where the response from the initial
   * purchase request times out or the 'purchaseId' isn't received, having the `merchantRef` allows the
   * merchant to retrieve detailed transaction information. This ensures they can track and access the
   * specific purchase details related to that reference, even if the immediate response was not received.
   *
   *
   * However, if the merchant does not specify the 'merchantRef' parameter, the platform will
   * automatically assign and use an internal 'purchaseId' as a reference for that transaction.
   *
   * ### paymentMethod
   *
   * The `paymentMethod` parameter identifies which payment solution merchant wants to use to perform a
   * transaction. If parameter is provided, than Paysecure will perform direct payment with the selected
   * payment method instead of loading Paysecure URL.
   *
   * ### Flow Diagram:
   *
   * <img src="https://content.pstmn.io/2dd13473-b015-4a40-b71a-542585919bcc/RW5jcnlwdGVkIEZsb3cucG5n">
   *
   * @param body
   * @return Response from the API call
   */
  async purchasesEncryptedFlow(
    body: PurchasesEncryptedFlowRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseSuccess4>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, purchasesEncryptedFlowRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(purchaseSuccess4Schema, requestOptions);
  }

  /**
   * To process a Google Pay transaction, a Purchase must first be created using the /purchases API. The
   * Google Pay token must be included in the request under extraParam.googlepay_param.
   *
   * To generate a Purchase, you are required to provide the `Brand ID` (in the request body) and `API
   * key` (in the header) Both can be located in the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a purchase
   * request.
   *
   * ### **Parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.full_name | Conditional.  <br>But Recommended to pass the complete name of the client.
   * Atleast 2 words recommended. |
   * | client.email | Conditional.  <br>But Recommended to pass a correct email id. |
   * | paymentMethod | Conditional.  <br>Mandatory if you are not using Paysecure Cashier. Else Non
   * Mandatory.  <br>Value: GOOGLEPAY |
   * | purchase.currency | Mandatory.  <br>ISO 4217 code for currency you want to send the transaction in.
   * This should be 'BRL' for PIX. |
   * | purchase.products.name | Mandatory. |
   * | purchase.products.price | Mandatory.  <br>Price in decimal format upto 2 decimals.  <br>example 1:
   * <br>EUR 5 , should be sent as 5.00  <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.
   * 37 |
   * | brand_id | Mandatory.  <br>Obtain from Dashboard section of your merchant account login. |
   * | success_redirect | Mandatory.  <br>URL to send the user if the transactions is successful. |
   * | pending_redirect | Mandatory.  <br>URL to send the user if the transactions is in progress state.
   * |
   * | failure_redirect | Mandatory  <br>URL to send the user if the transactions has failed. |
   *
   * ## Optional Parameters
   *
   * ### expireInMin
   *
   * This parameter, when passed in the PayIn request, sets the time window within which a customer
   * should make the Interac payment through their bank's app. If a customer doesn't complete the payment
   * within the configured time window, the status of the payment will be marked as 'EXPIRED' in
   * Paysecure.
   *
   * However, in certain cases, a payment made outside of this window could still be successful i.e.
   * customer's account may be debited which would result in inconsistencies among Paysecure's,
   * merchant's and customer's status of the transaction. To avoid such cases it is advised to set the
   * expiry time limit appropriately and advise the customers to make payment within this window. However,
   * if there is an inconsistency between the status at Paysecure and at customer's Bank, the merchant
   * and Paysecure will reconcile the amounts during the settlement.
   *
   * ### merchantRef
   *
   * The `merchantRef` parameter, although not mandatory, is strongly advised for merchants to specify.
   * Its inclusion offers two significant advantages for the merchant:
   *
   * 1. **Prevention of Duplicate Requests:** When a `merchantRef` is utilized, it acts as a unique
   * identifier. This means that if a second purchase request is made using the same `merchantRef`
   * (reference number), the system will recognize it and prevent duplicate transactions from occurring.
   * This prevents unintended or duplicate purchases.
   *
   * 2. **Facilitation of Transaction Retrieval:** In situations where the response from the initial
   * purchase request times out or the 'purchaseId' isn't received, having the `merchantRef` allows the
   * merchant to retrieve detailed transaction information. This ensures they can track and access the
   * specific purchase details related to that reference, even if the immediate response was not received.
   *
   *
   * However, if the merchant does not specify the 'merchantRef' parameter, the platform will
   * automatically assign and use an internal 'purchaseId' as a reference for that transaction.
   *
   * ### paymentMethod
   *
   * The `paymentMethod` parameter identifies which payment solution merchant wants to use to perform a
   * transaction. If parameter is provided, than Paysecure will perform direct payment with the selected
   * payment method instead of loading Paysecure URL.
   *
   * ### Flow Diagram:
   *
   * <img src="https://content.pstmn.io/8d726da8-02f8-49a4-a3a9-765cd3e1dd9a/RGVjcnlwdGVkIEZsb3cucG5n">
   *
   * @param body
   * @return Response from the API call
   */
  async purchasesDecryptedFlow(
    body: PurchasesDecryptedFlowRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseSuccess4>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, purchasesDecryptedFlowRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(purchaseSuccess4Schema, requestOptions);
  }

  /**
   * This API tells you about all the details of a purchase (PayIn), including its history
   *
   * ### Mandatory
   *
   * you need to pass the `PurchaseID` in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | paid | Transaction Successful |
   * | payment_in_process | Payment is under Processing |
   * | expired | Purchase has Expired. |
   * | error | Transaction has Failed. |
   *
   * ### Response Examples
   *
   * In the response examples you can see instances of both `Paid` and `Error` Status purchases.
   *
   * ### Error Details
   *
   * If there are any issues on the Purchase you can get more details from the `transaction_data.attempts.
   * error` attribute of the response object
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * Please see the Status Code section for further details.
   *
   * @param purchaseId
   * @return Response from the API call
   */
  async getStatus2(
    purchaseId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseSuccess4>> {
    const req = this.createRequest('GET');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ purchaseId: [purchaseId, string()] });
    req.header('Content-Type', 'application/json');
    req.appendTemplatePath`/api/v1/purchases/${mapped.purchaseId}`;
    req.authenticate([]);
    return req.callAsJson(purchaseSuccess4Schema, requestOptions);
  }

  /**
   * To process an Apple Pay transaction, a Purchase must be created using the /purchases API. The Apple
   * Pay token must be included under extraParam.applepay_param.
   *
   * To generate a Purchase, you are required to provide the `Brand ID` (in the request body) and `API
   * key` (in the header) Both can be located in the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a purchase
   * request.
   *
   * ### **Parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.full_name | Conditional.  <br>But Recommended to pass the complete name of the client.
   * Atleast 2 words recommended. |
   * | client.email | Conditional.  <br>But Recommended to pass a correct email id. |
   * | paymentMethod | Conditional.  <br>Mandatory if you are not using Paysecure Cashier. Else Non
   * Mandatory.  <br>Value: APPLEPAY |
   * | purchase.currency | Mandatory.  <br>ISO 4217 code for currency you want to send the transaction in.
   * This should be 'BRL' for PIX. |
   * | purchase.products.name | Mandatory. |
   * | purchase.products.price | Mandatory.  <br>Price in decimal format upto 2 decimals.  <br>example 1:
   * <br>EUR 5 , should be sent as 5.00  <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.
   * 37 |
   * | brand_id | Mandatory.  <br>Obtain from Dashboard section of your merchant account login. |
   * | success_redirect | Mandatory.  <br>URL to send the user if the transactions is successful. |
   * | pending_redirect | Mandatory.  <br>URL to send the user if the transactions is in progress state.
   * |
   * | failure_redirect | Mandatory  <br>URL to send the user if the transactions has failed. |
   *
   * ## Optional Parameters
   *
   * ### expireInMin
   *
   * This parameter, when passed in the PayIn request, sets the time window within which a customer
   * should make the Interac payment through their bank's app. If a customer doesn't complete the payment
   * within the configured time window, the status of the payment will be marked as 'EXPIRED' in
   * Paysecure.
   *
   * However, in certain cases, a payment made outside of this window could still be successful i.e.
   * customer's account may be debited which would result in inconsistencies among Paysecure's,
   * merchant's and customer's status of the transaction. To avoid such cases it is advised to set the
   * expiry time limit appropriately and advise the customers to make payment within this window. However,
   * if there is an inconsistency between the status at Paysecure and at customer's Bank, the merchant
   * and Paysecure will reconcile the amounts during the settlement.
   *
   * ### merchantRef
   *
   * The `merchantRef` parameter, although not mandatory, is strongly advised for merchants to specify.
   * Its inclusion offers two significant advantages for the merchant:
   *
   * 1. **Prevention of Duplicate Requests:** When a `merchantRef` is utilized, it acts as a unique
   * identifier. This means that if a second purchase request is made using the same `merchantRef`
   * (reference number), the system will recognize it and prevent duplicate transactions from occurring.
   * This prevents unintended or duplicate purchases.
   *
   * 2. **Facilitation of Transaction Retrieval:** In situations where the response from the initial
   * purchase request times out or the 'purchaseId' isn't received, having the `merchantRef` allows the
   * merchant to retrieve detailed transaction information. This ensures they can track and access the
   * specific purchase details related to that reference, even if the immediate response was not received.
   *
   *
   * However, if the merchant does not specify the 'merchantRef' parameter, the platform will
   * automatically assign and use an internal 'purchaseId' as a reference for that transaction.
   *
   * ### paymentMethod
   *
   * The `paymentMethod` parameter identifies which payment solution merchant wants to use to perform a
   * transaction. If parameter is provided, than Paysecure will perform direct payment with the selected
   * payment method instead of loading Paysecure URL.
   *
   * @param body
   * @return Response from the API call
   */
  async purchasesEncryptedFlow1(
    body: PurchasesEncryptedFlowRequest1,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseSuccess4>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, purchasesEncryptedFlowRequest1Schema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(purchaseSuccess4Schema, requestOptions);
  }

  /**
   * To process an Apple Pay transaction, a Purchase must be created using the /purchases API. The Apple
   * Pay token must be included under extraParam.applepay_param.
   *
   * To generate a Purchase, you are required to provide the `Brand ID` (in the request body) and `API
   * key` (in the header) Both can be located in the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a purchase
   * request.
   *
   * ### **Parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.full_name | Conditional.  <br>But Recommended to pass the complete name of the client.
   * Atleast 2 words recommended. |
   * | client.email | Conditional.  <br>But Recommended to pass a correct email id. |
   * | paymentMethod | Conditional.  <br>Mandatory if you are not using Paysecure Cashier. Else Non
   * Mandatory.  <br>Value: APPLEPAY |
   * | purchase.currency | Mandatory.  <br>ISO 4217 code for currency you want to send the transaction in.
   * This should be 'BRL' for PIX. |
   * | purchase.products.name | Mandatory. |
   * | purchase.products.price | Mandatory.  <br>Price in decimal format upto 2 decimals.  <br>example 1:
   * <br>EUR 5 , should be sent as 5.00  <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.
   * 37 |
   * | brand_id | Mandatory.  <br>Obtain from Dashboard section of your merchant account login. |
   * | success_redirect | Mandatory.  <br>URL to send the user if the transactions is successful. |
   * | pending_redirect | Mandatory.  <br>URL to send the user if the transactions is in progress state.
   * |
   * | failure_redirect | Mandatory  <br>URL to send the user if the transactions has failed. |
   *
   * ## Optional Parameters
   *
   * ### expireInMin
   *
   * This parameter, when passed in the PayIn request, sets the time window within which a customer
   * should make the Interac payment through their bank's app. If a customer doesn't complete the payment
   * within the configured time window, the status of the payment will be marked as 'EXPIRED' in
   * Paysecure.
   *
   * However, in certain cases, a payment made outside of this window could still be successful i.e.
   * customer's account may be debited which would result in inconsistencies among Paysecure's,
   * merchant's and customer's status of the transaction. To avoid such cases it is advised to set the
   * expiry time limit appropriately and advise the customers to make payment within this window. However,
   * if there is an inconsistency between the status at Paysecure and at customer's Bank, the merchant
   * and Paysecure will reconcile the amounts during the settlement.
   *
   * ### merchantRef
   *
   * The `merchantRef` parameter, although not mandatory, is strongly advised for merchants to specify.
   * Its inclusion offers two significant advantages for the merchant:
   *
   * 1. **Prevention of Duplicate Requests:** When a `merchantRef` is utilized, it acts as a unique
   * identifier. This means that if a second purchase request is made using the same `merchantRef`
   * (reference number), the system will recognize it and prevent duplicate transactions from occurring.
   * This prevents unintended or duplicate purchases.
   *
   * 2. **Facilitation of Transaction Retrieval:** In situations where the response from the initial
   * purchase request times out or the 'purchaseId' isn't received, having the `merchantRef` allows the
   * merchant to retrieve detailed transaction information. This ensures they can track and access the
   * specific purchase details related to that reference, even if the immediate response was not received.
   *
   *
   * However, if the merchant does not specify the 'merchantRef' parameter, the platform will
   * automatically assign and use an internal 'purchaseId' as a reference for that transaction.
   *
   * ### paymentMethod
   *
   * The `paymentMethod` parameter identifies which payment solution merchant wants to use to perform a
   * transaction. If parameter is provided, than Paysecure will perform direct payment with the selected
   * payment method instead of loading Paysecure URL.
   *
   * @param body
   * @return Response from the API call
   */
  async purchasesDecryptedFlow1(
    body: PurchasesDecryptedFlowRequest1,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseSuccess4>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, purchasesDecryptedFlowRequest1Schema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(purchaseSuccess4Schema, requestOptions);
  }

  /**
   * This API tells you about all the details of a purchase (PayIn), including its history
   *
   * ### Mandatory
   *
   * you need to pass the `PurchaseID` in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | paid | Transaction Successful |
   * | payment_in_process | Payment is under Processing |
   * | expired | Purchase has Expired. |
   * | error | Transaction has Failed. |
   *
   * ### Response Examples
   *
   * In the response examples you can see instances of both `Paid` and `Error` Status purchases.
   *
   * ### Error Details
   *
   * If there are any issues on the Purchase you can get more details from the `transaction_data.attempts.
   * error` attribute of the response object
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * Please see the Status Code section for further details.
   *
   * @param purchaseId
   * @return Response from the API call
   */
  async getStatus3(
    purchaseId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseSuccess4>> {
    const req = this.createRequest('GET');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ purchaseId: [purchaseId, string()] });
    req.header('Content-Type', 'application/json');
    req.appendTemplatePath`/api/v1/purchases/${mapped.purchaseId}`;
    req.authenticate([]);
    return req.callAsJson(purchaseSuccess4Schema, requestOptions);
  }

  /**
   * To Initiate a payment, the very first call to make is `/purchases` with the required data in the
   * request body.
   *
   * To generate a Purchase, you are required to provide the `Brand ID` (in the request body) and `API
   * key` (in the header) Both can be located in the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a purchase
   * request.
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.email | The customer's email. |
   * | client.city | The customer's city. |
   * | client.country | ISO-3166 Country Code. Must be upper case. Example “SG” (Alpha2) |
   * | client.stateCode | Example “AL”, “XZ”. Must be in upper case. |
   * | Client.street_address | The customer's address. |
   * | client.zip_code | The customer's ZIP or postal code. If country=US, zip format must be NNNNN or
   * NNNNN-NNNN. |
   * | purchase.currency | ISO 4217 code for currency you want to send the transaction in.  <br>
   * <br>Please note, the currency has to be enabled by the account manager for your account. |
   * | purchase.products | An object which contains the list of products which the customer is buying. |
   * | purchase.products.name | The name of the product. |
   * | purchase.products.price | Price in decimal format.  <br>example 1:  <br>EUR 5 , should be sent as
   * 5.00  <br>  <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.37 |
   * | brand_id | Obtain from Dashboard section of your merchant account login. |
   * | success_redirect | URL to send the user if the transactions is successful. |
   * | pending_redirect | URL to send the user if the transactions is in pending. |
   * | failure_redirect | URL to send the user if the transactions is unsuccessful. |
   *
   * ## Essential Optional Parameters
   *
   * ### merchantRef
   *
   * The `merchantRef` parameter, although not mandatory, is strongly advised for merchants to specify.
   * Its inclusion offers two significant advantages for the merchant:
   *
   * 1. **Prevention of Duplicate Requests:** When a `merchantRef` is utilized, it acts as a unique
   * identifier. This means that if a second purchase request is made using the same `merchantRef`
   * (reference number), the system will recognize it and prevent duplicate transactions from occurring.
   * This prevents unintended or duplicate purchases.
   *
   * 2. **Facilitation of Transaction Retrieval:** In situations where the response from the initial
   * purchase request times out or the 'purchaseId' isn't received, having the `merchantRef` allows the
   * merchant to retrieve detailed transaction information. This ensures they can track and access the
   * specific purchase details related to that reference, even if the immediate response was not received.
   *
   *
   * However, if the merchant does not specify the 'merchantRef' parameter, the platform will
   * automatically assign and use an internal 'purchaseId' as a reference for that transaction.
   *
   * Upon successul create on a Purchase you'd get a `"purchaseId"` . Use this ID for the next step in
   * Server to server call by calling `p/{purchaseId}/?s2s=true .`
   *
   * ### paymentMethod
   *
   * The `paymentMethod` parameter identifies which payment solution merchant wants to use to perform a
   * transaction. If parameter is provided, than Paysecure will perform direct payment with the selected
   * payment method instead of loading Paysecure URL.
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * ### Possible Error Messages
   *
   * | **Error Messages** |
   * | --- |
   * | Allowed Limit for this card for particular time period has been consumed |
   * | some Mandatory Parameter are missing |
   * | Invalid format of Date_of_Birth \[allowed format: yyyy-mm-dd\] |
   * | Invalid Email Format |
   * | Please submit Valid Alpha2 Country Code Ex:(AF,IN) in \\"country\\" parameter |
   * | Please pass Valid State Code |
   * | Please pass valid street address in \\"street_address\\" parameter |
   * | Please pass valid city name in \\"city\\" parameter |
   * | Please pass valid postal Code name in \\"zip_code\\" parameter |
   * | You are not Allowed for Live Transaction |
   * | Merchant Limit is not set |
   * | Minimum amount is not set for this merchant |
   * | Brand not found ! |
   * | Your charges setting is incomplete .Plese Contact to Administrator. |
   * | Only {{currencyCode}} currency is allowed. |
   * | Transaction amount must be equal or greater to minimum trans amount |
   * | Invalid_Parameter  <br>You'd usually get it when one or more mandatory parameters are not present
   * in the request. |
   *
   * @param body
   * @return Response from the API call
   */
  async purchase1(
    body: PurchaseRequest2,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<OpenBankingEuUk>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ body: [body, purchaseRequest2Schema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(openBankingEuUkSchema, requestOptions);
  }

  /**
   * This API tells you about all the details of a purchase, including its history
   *
   * ### Mandatory
   *
   * you need to pass the `PurchaseID` in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | created | When Purchase order is created. |
   * | paid | Transaction Successful |
   * | payment_in_process | Payment is under Processing |
   * | chargeback | Payment is chargeback state |
   * | refund_in_process | Refund is under Processing. |
   * | expired | Purchase has Expired. |
   * | overdue | Purchase is Overdued. |
   * | cancelled | Purchase is cancelled. |
   * | error | Transaction has Failed. |
   * | refunded | Payment is refunded |
   *
   * ### Response Examples
   *
   * In the response examples you can see instances of both `Paid` and `Error` Status purchases.
   *
   * ### Error Details
   *
   * If there are any issues on the Purchase you can get more details from the `transaction_data.attempts.
   * error` attribute of the response object
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * Please see the Status Code section for further details.
   *
   * @return Response from the API call
   */
  async getStatus11(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchasesSuccess>> {
    const req = this.createRequest(
      'GET',
      '/api/v1/purchases/64bff12bf63e36669499e97d'
    );
    req.baseUrl('Server 3');
    req.header('Content-Type', 'application/json');
    req.authenticate([]);
    return req.callAsJson(purchasesSuccessSchema, requestOptions);
  }

  /**
   * This API is used to initate refund of the purchases that have status as `PAID` can be initiated for
   * refund
   *
   * Once the refund has been initiated, the status of the purchase would become `REFUND_IN_PROCESS` and
   * once the refund has been approved by the bank, the status woud become `REFUNDED`
   *
   * Purchase ID is **mandatory** for this API.
   *
   * Currently we don't support partial refunds.
   *
   * ### Response Examples
   *
   * In the response examples you can see instances of both `successful` and `unsuccessful` in refund of
   * purchases.
   *
   * @return Response from the API call
   */
  async refund1(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RefundSuccess1>> {
    const req = this.createRequest(
      'GET',
      '/api/v1/purchases/657af976c3fceb7beb30eeb4/refund'
    );
    req.baseUrl('Server 3');
    req.header('Content-Type', 'application/json');
    req.throwOn(400, RefundError, 'Bad Request');
    req.authenticate([]);
    return req.callAsJson(refundSuccess1Schema, requestOptions);
  }

  /**
   * **OVERIEW**
   *
   * The **createCustomer API** allows merchants to create a customer profile on Paysecure when a user
   * visits their website, facilitating a seamless payment experience by pre-filling the customer's
   * details, on the Paysecure Cashier.
   *
   * The API takes in customer information and returns a unique customerID that can be passed in purchase
   * payload, eliminating the need for customers to manually enter their details during transactions.
   *
   * | Parameter | Type | **Required** | Description | Example |
   * | --- | --- | --- | --- | --- |
   * | merchantCustomerId | String | Yes | unique customer ID provided by merchant. This can be email,
   * mobile or an alphanumeric string | [rahultestcustomer@paysecure.net](https://mailto:
   * rahultestcustomer@paysecure.net) |
   * | fullName | String | Yes | Full name of the customer. | Rahul Agarwal |
   * | emailId | String | Yes | Email address of the customer. | [test@paysecure.net](https://mailto:
   * test@paysecure.net) |
   * | dateOfBirth | String | Optional | Customer's date of birth in YYYY-MM-DD format. | 1995-12-27 |
   * | phoneNo | String | Yes | Customer's phone number. Preferred with the country code | +91 797639082
   * |
   * | city | String | Optional | City of the customer. | Jaipur |
   * | stateCode | String | Yes | State code of the customer. | RJ |
   * | zipCode | String | Optional | Postal code of the customer's location. | 302018 |
   * | address | String | Optional | Full address of the customer. | House Number 53, Vaishali Nagar,
   * Jaipur |
   * | country | String | Yes | Country code (ISO 3166-1 alpha-2). | IN |
   * | custRegDate | Sting | Optional | Customer registration date on merchant site in YYYY-MM-DD format.
   * | 2023-12-27 |
   * | successTxn | String | Optional | Number of successful transactions customer has carried on
   * merchant site. | 32 |
   *
   * ### **User Flow**
   *
   * 1. **Customer visits merchant's website**: The customer selects the product or service and proceeds
   * to checkout page.
   *
   * 2. **Merchant calls the** **`createCustomer`** **API**: The merchant sends a `POST` request to the
   * Paysecure API with the customer's details.
   *
   * 3. **Customer ID generation**: Paysecure processes the request and generates a unique `customerId`
   * which is returned in the response.
   *
   * 4. **Payment Process**: The merchant uses the `customerId` for subsequent session creation, ensuring
   * the customer doesn’t need to re-enter their details for future payments.
   *
   *
   * ---
   *
   * ### **Benefits**
   *
   * - **Seamless user experience**: The customer does not have to enter their details manually for every
   * purchase.
   *
   * - **Faster Checkout**: The merchant can use the `customerId` for faster payment processing.
   *
   * - **Data Security**: Customer details are stored securely and can be reused across transactions,
   * reducing the chances of errors.
   *
   *
   * ### Implementing createCustomer on merchant site:
   *
   * The `createCustomer` can be implemented on merchant site in one of two ways, depending on merchant’s
   * integration flow:
   *
   * **At Checkout:**
   *
   * When a customer initiates the checkout process, your system should invoke the `createCustomer` API,
   * before actually starting the session.
   *
   * - **Call** **`createCustomer`**: This API call generates a unique customer ID.
   *
   * - **Use of Customer ID:** Pass the returned customer ID in Session API, when creating a payment
   * session.
   *
   *
   * **Upon Customer Login:**
   *
   * Alternatively, merchants may choose to call `createCustomer` at the time of customer login on their
   * site.
   *
   * - **Persistent Customer Profiles:** This approach can help build or maintain a persistent customer
   * profile, ensuring the customer ID is available for any subsequent payment sessions without needing
   * to call `createCustomer` again.
   *
   * - **Integration Flexibility:** This is useful for merchants who want to initialize customer data
   * early in the customer journey rather than during checkout.
   *
   *
   * The method you choose depends on your business requirements and how your system is architected.
   * Regardless of when you call it, the resulting customer ID must be passed to the `createSession` API
   * to initiate a payment session.
   *
   * @param brandId
   * @param body
   * @return Response from the API call
   */
  async createCustomer(
    brandId: string,
    body: CreateCustomerRequest1,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success19>> {
    const req = this.createRequest('POST', '/api/v1/customer');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      brandId: [brandId, string()],
      body: [body, createCustomerRequest1Schema],
    });
    req.header('BrandId', mapped.brandId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success19Schema, requestOptions);
  }

  /**
   * This endpoint makes an HTTP GET request to retrieve customer information from the Paysecure API.
   *
   * ### Request
   *
   * The request does not include any query parameters, but it uses a raw request body with the following
   * parameter "merchantCustomerId", which will be the customer id on the merchant's side.
   *
   * ### Response
   *
   * The response of this request can be documented as a JSON schema.
   *
   * @param brandId
   * @param accept
   * @return Response from the API call
   */
  async getCustomer(
    brandId: string,
    accept: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success19>> {
    const req = this.createRequest('GET', '/customer');
    req.baseUrl('Server 2');
    const mapped = req.prepareArgs({
      brandId: [brandId, string()],
      accept: [accept, string()],
    });
    req.header('BrandId', mapped.brandId);
    req.header('Content-Type', 'application/json');
    req.header('Accept', mapped.accept);
    req.authenticate([]);
    return req.callAsJson(success19Schema, requestOptions);
  }

  /**
   * **OVERIEW**
   *
   * The `createSession` API is designed to initiate a payment session for a customer after their profile
   * has been created via the **createCustomer** API. The session is automatically generated at the
   * backend on the merchant’s checkout URL. This session allows the customer to continue with their
   * transaction without having to manually interact with the session URL, and ensures the session
   * remains active until the payment is completed or the session expires.
   *
   * This API improves the user experience by reducing transaction failures and enhancing the overall
   * payment process, thus reducing transaction churn rates.
   *
   * ### Mandatory Parameters
   *
   * | Parameter | Type | Description | Example |
   * | --- | --- | --- | --- |
   * | customerId | String | The unique ID of the customer (generated from createCustomer). |
   * 6731a609b6bb5a43ad66c4a6 |
   *
   * To register a mandate, pass mandate related parameters in the Create Session API to create a mandate
   * registration purchase. Post transaction completion, use Get Status API and Webhooks to capture
   * mandate registration details.
   *
   * ### Additional **parameters for this usecase:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | payment_type | Mandatory.  <br>To be passed as "mandate". |
   * | mandate | Mandatory.  <br>This object is mandatory for mandate flow |
   * | mandate.mandate_max_amount | Optional  <br>Maximum amount for a mandate. |
   * | mandate.amount_variability | Optional  <br>Specifies whether the amount is fixed or variable for
   * each recurrence.  <br>  <br>Possible values: Fixed or Variable |
   * | mandate.start_date | Mandatory  <br>Specifies the time when the mandate will start. |
   * | mandate.end_date | Mandatory  <br>Specifies the time when the mandate will end. |
   * | mandate.frequency | Mandatory  <br>Defines the frequency of mandate execution, how often a
   * customer should be charged.  <br>  <br>Possible Values:  <br>**ONETIME**: This is for a one-time
   * mandate. The customer provides authorisation to debit their account a single time for a specific
   * amount.  <br>  <br>**DAILY**: Recurring mandate. The customer provides authorisation to debit their
   * account daily for a specific amount.  <br>  <br>**WEEKLY**: Recurring mandate. Authorisation given
   * by a customer to debit their account weekly once for a specified amount.  <br>  <br>**FORTNIGHTLY**:
   * Recurring mandate. The customer provides authorisation to debit their account fortnightly for a
   * specific amount.  <br>  <br>**MONTHLY**: Recurring mandate. The customer provides authorisation to
   * debit their account monthly for a specific amount.  <br>  <br>**BIMONTHLY**: Recurring mandate. The
   * customer provides authorisation to debit their account bimonthly for a specific amount.  <br>
   * <br>**QUARTERLY**: Recurring mandate. The customer provides authorisation to debit their account
   * quarterly for a specific amount.  <br>  <br>**HALFYEARLY**: Recurring mandate. The customer provides
   * authorisation to debit their account once in every 6 months for a specific amount.  <br>
   * <br>**YEARLY**: Recurring mandate. The customer provides authorisation to debit their account once a
   * year for a specific amount.  <br>  <br>**ASPRESENTED**: For cases where the scheduling is handled by
   * the merchant. The customer provides authorization to debit their account whenever there is an
   * execution request. |
   * | mandate.rule | Optional.  <br>Indicates the recurrence rule of the mandate. This rule is not
   * required for onetime, daily, and aspresented recurrence patterns.  <br>Possible values:
   * on/before/after  <br>By Default: on |
   * | mandate.value | Conditional.  <br>The recurrence Value of the mandate.  <br>It is not required for
   * ONETIME, DAILY and ASPRESENTED frequencies.  <br>  <br>Possible values:  <br>For WEEKLY: The value
   * should be 1-Monday to 7-Sunday.  <br>For MONTHLY, BIMONTHLY, QUARTERLY, HALFYEARLY or YEARLY: The
   * value should be between 1 to 30/31 days.  <br>  <br>By Default: The value will be treated as 1. |
   *
   * ### **Usage Flow**
   *
   * 1. **Customer completes the** **`createCustomer`** **API**: Once the customer is created via the
   * `createCustomer` API, the merchant proceeds to call the `createSession` API to generate a session.
   *
   * 2. **Merchant sends** **`createSession`** **request**: The merchant sends a `POST` request to the
   * `createSession` API with the customer’s `customerId` and the merchant’s `brandId`.
   *
   * 3. **Session created automatically on the checkout URL**: PaySecure creates the session and
   * associates it with the merchant’s checkout page. This session remains active on the backend,
   * allowing the customer to continue the payment process without interruption.
   *
   * 4. **Customer proceeds to checkout**: The customer is redirected to the checkout page as part of the
   * merchant's payment flow, where the session is automatically validated. The session remains active
   * for a set period, allowing the customer to complete the payment without needing to manually interact
   * with the session URL.
   *
   * 5. **Session Expiry**: The session expires after the set expiration time (`expiryOn`), ensuring the
   * payment process is completed within the defined window. Default is 15 mins from the creation time.
   *
   *
   * ---
   *
   * ### **Benefits**
   *
   * - **Seamless Checkout**: The session URL is automatically generated and active at the backend,
   * ensuring a smoother and uninterrupted customer experience.
   *
   * - **Reduced Transaction Failures**: By ensuring the session remains active during the payment
   * process, the chances of transaction failures due to session timeouts are minimized.
   *
   * - **Improved User Experience**: The customer does not need to click on a session URL or navigate
   * away from the page, leading to faster and more secure payment processing.
   *
   * @param brandId
   * @param body
   * @return Response from the API call
   */
  async createSessionCit(
    brandId: string,
    body: CreateSessionCitRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success32>> {
    const req = this.createRequest('POST', '/api/v1/createSession');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      brandId: [brandId, string()],
      body: [body, createSessionCitRequestSchema],
    });
    req.header('BrandId', mapped.brandId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success32Schema, requestOptions);
  }

  /**
   * **OVERIEW**
   *
   * The `create Purchase` API is designed to initiate a payment for a merchant who doesn't want to use
   * Paysecure's cashier and wants to keep everything S2S.
   *
   * The merchant doesn't need to create a customer in this case and directly needs to call the purchase
   * API and then call the S2S API after that.
   *
   * ### Additional **parameters for this usecase:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | payment_type | Mandatory.  <br>To be passed as "mandate". |
   * | paymentMethod | Mandatory.  <br>Example: MASTER, VISA |
   * | brand_id | Mandatory.  <br>Your brand id. |
   * | mandate | Mandatory.  <br>This object is mandatory for mandate flow |
   * | mandate.mandate_max_amount | Conditional.  <br>Mandatory when amount_variability is "Variable".
   * <br>Maximum amount for a mandate. |
   * | mandate.amount_variability | Optional  <br>Specifies whether the amount is fixed or variable for
   * each recurrence.  <br>  <br>Possible values: Fixed or Variable |
   * | mandate.start_date | Optional  <br>Specifies the time when the mandate will start. |
   * | mandate.end_date | Optional  <br>Specifies the time when the mandate will end.  <br>(If not passed
   * then indefinite) |
   * | mandate.frequency | Mandatory  <br>Defines the frequency of mandate execution, how often a
   * customer should be charged.  <br>  <br>Possible Values:  <br>**ONETIME**: This is for a one-time
   * mandate. The customer provides authorisation to debit their account a single time for a specific
   * amount.  <br>  <br>**DAILY**: Recurring mandate. The customer provides authorisation to debit their
   * account daily for a specific amount.  <br>  <br>**WEEKLY**: Recurring mandate. Authorisation given
   * by a customer to debit their account weekly once for a specified amount.  <br>  <br>**FORTNIGHTLY**:
   * Recurring mandate. The customer provides authorisation to debit their account fortnightly for a
   * specific amount.  <br>  <br>**MONTHLY**: Recurring mandate. The customer provides authorisation to
   * debit their account monthly for a specific amount.  <br>  <br>**BIMONTHLY**: Recurring mandate. The
   * customer provides authorisation to debit their account bimonthly for a specific amount.  <br>
   * <br>**QUARTERLY**: Recurring mandate. The customer provides authorisation to debit their account
   * quarterly for a specific amount.  <br>  <br>**HALFYEARLY**: Recurring mandate. The customer provides
   * authorisation to debit their account once in every 6 months for a specific amount.  <br>
   * <br>**YEARLY**: Recurring mandate. The customer provides authorisation to debit their account once a
   * year for a specific amount.  <br>  <br>**ASPRESENTED**: For cases where the scheduling is handled by
   * the merchant. The customer provides authorization to debit their account whenever there is an
   * execution request. |
   * | mandate.rule | Optional.  <br>Indicates the recurrence rule of the mandate. This rule is not
   * required for onetime, daily, and aspresented recurrence patterns.  <br>Possible values:
   * on/before/after  <br>By Default: on |
   * | mandate.value | Conditional.  <br>The recurrence Value of the mandate.  <br>It is not required for
   * ONETIME, DAILY and ASPRESENTED frequencies.  <br>  <br>Possible values:  <br>For WEEKLY: The value
   * should be 1-Monday to 7-Sunday.  <br>For MONTHLY, BIMONTHLY, QUARTERLY, HALFYEARLY or YEARLY: The
   * value should be between 1 to 30/31 days.  <br>  <br>By Default: The value will be treated as 1. |
   *
   * @param body
   * @return Response from the API call
   */
  async createPurchaseCit(
    body: CreatePurchaseCitRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      body: [body, createPurchaseCitRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(401, Success33Error, 'Unauthorized');
    req.authenticate([]);
    return req.call(requestOptions);
  }

  /**
   * This API would be the 2nd in sequence to call if the call to `/purchases/` was successful in step 1.
   * and you want to do the transaction via Server-to-Server mode.
   *
   * The request body would contain the details of the card that's to be transacted upon.
   *
   * ### **Mandatory parameters in request body:**
   *
   * | Parameter | **Notes** |
   * | --- | --- |
   * | remote_ip | IP address of the customer’s device used for transaction. Helps in fraud checks and
   * risk scoring. Ex- 157.38.242.7 |
   * | user_agent | Full browser and operating system details captured from HTTP header. EX- Chrome/5.0
   * (X11; Linux x86_64) |
   * | accept_header | Browser’s accepted content types (from HTTP header). Used for validating request
   * origin and device info. EX- text/html |
   *
   * ### **Essential parameters in request body:**
   *
   * | Parameter | **Notes** |
   * | --- | --- |
   * | cardholder_name | Name of the cardholder |
   * | card_number | The card numbers Must be 10-20 characters. |
   * | expires | must be greater than the current month/year. |
   * | cvc | 3 or 4 digit |
   *
   * **
   * Other optional parameters in request body:**
   *
   * | Parameter | **Notes** |
   * | --- | --- |
   * | language | Preferred language of the user’s browser . EX - en-US |
   * | java_enabled | Indicates if Java is enabled in the browser. Helps PSPs in device profiling. Ex-
   * true/false |
   * | javascript_enabled | Indicates if JavaScript is enabled in the client browser. Used for 3DS or
   * risk-based checks. Ex- true |
   * | color_depth | Bit depth of the display screen. Ex- 24 |
   * | utc_offset | Difference in minutes between local time and UTC. Used for location and timezone
   * checks. Ex- 330 |
   * | screen_width | Width of the device screen in pixels. Used in device profiling. Ex- 1920 |
   * | screen_height | Height of the device screen in pixels. Ex- 1080 |
   *
   * **Successful Response**
   *
   * If all the details are correct you'll get a 202 response with staus as Pending
   *
   * ``` json
   * {
   * "status": "pending",
   * "callback_url": "https://paysecure.net/payment/63bd0bf80fb42a076e8a4dd1/",
   * "method": "GET"
   * }
   *
   * ```
   *
   * If the response code is 202, after receiving the response body, direct the customer to the
   * callback_url provided in the response.
   *
   * **Errors**
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * Please see the Status Code section for further details.
   *
   * | **Error Messages** |
   * | --- |
   * | Client Ip could not be matched with Merchant Ip |
   * | Invalid Card Information |
   * | Card is Blocked |
   * | Different Type of key used to create purchase and payment |
   * | You charges setting is incomplete .Plese Contact to adminstrartor. |
   * | Allowed Attempt for this Transaction has been consumed |
   * | Invalid Card Expiry(Valid Format:MM/YY) must be greator than current month/year |
   * | Customer profile is Blocked |
   * | Customer/Card not allowed for transaction |
   *
   * @param s2S
   * @param body
   * @return Response from the API call
   */
  async s2SApi(
    s2S: boolean,
    body: S2SApiRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success1>> {
    const req = this.createRequest(
      'POST',
      '/api/v1/p/64bff4fff63e36669499e9c8'
    );
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      s2S: [s2S, boolean()],
      body: [body, s2SApiRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.query('s2s', mapped.s2S);
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success1Schema, requestOptions);
  }

  /**
   * This endpoint will be used to get all the details of a particular mandate. You can search for a
   * particular mandate id or for a particular session id.
   *
   * ### Response **parameters:**
   *
   * 1. **totalDeductedAmount**
   * This is the total amount which is deducted from the user's account.
   *
   * 2. **recurringMandateIds**
   * This object returns a list of all the MIT transactions done for the respective mandate.
   *
   * 3. **nextMandateDate**
   * This is the date when the next recurring payment will happen.
   *
   * @param accept
   * @param brandId
   * @return Response from the API call
   */
  async getMandate(
    accept: string,
    brandId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest(
      'GET',
      '/api/v1/mandate/{mandate_id or session_id}'
    );
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      accept: [accept, string()],
      brandId: [brandId, string()],
    });
    req.header('Accept', mapped.accept);
    req.header('BrandId', mapped.brandId);
    req.throwOn(500, Success36Error, 'Internal Server Error');
    req.authenticate([]);
    return req.call(requestOptions);
  }

  /**
   * After the successful Mandate registration, Merchant will receive a mandate_id from Paysecure that
   * should be stored against a customer at their end.
   *
   * If the **merchant handles their own scheduling,** then for the subsequent transactions, the merchant
   * is supposed to pass a combination of customer_id and mandate_id to do a debit. This API will perform
   * a transaction with a PSP based on the mandate_id passed in the request.
   *
   * If **Paysecure handles the scheduling for the merchant** on their behalf, then this API is not for
   * them. The MIT transactions will be handled by Paysecure from our side.
   *
   * Note:
   * Recurring payments should only be done if the mandate registration has been successful and the
   * mandate is ACTIVE.
   *
   * @param accept
   * @param body
   * @return Response from the API call
   */
  async createPurchaseMit(
    accept: string,
    body: CreatePurchaseMitRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success38>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      accept: [accept, string()],
      body: [body, createPurchaseMitRequestSchema],
    });
    req.header('Accept', mapped.accept);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success38Schema, requestOptions);
  }

  /**
   * Required for cases where the Paysecure is handling the scheduling of recurring payments. Use this
   * API to revoke/cancel an ACTIVE mandate.
   *
   * Note:
   * Revoking a mandate should be done at the same instance this API is called. The mandate status should
   * be updated to “REVOKED” at the same instant.
   *
   * @param accept
   * @param brandid
   * @param body
   * @return Response from the API call
   */
  async revokeMandate(
    accept: string,
    brandid: string,
    body: RevokeMandateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success39>> {
    const req = this.createRequest('POST', '/api/v1/mandate');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      accept: [accept, string()],
      brandid: [brandid, string()],
      body: [body, revokeMandateRequestSchema],
    });
    req.header('Accept', mapped.accept);
    req.header('Brandid', mapped.brandid);
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success39Schema, requestOptions);
  }

  /**
   * Required for cases where the Paysecure is handling the scheduling of recurring payments. Use this
   * API to pause an ACTIVE mandate.
   *
   * ### Additional **parameters for this usecase:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | command | Mandatory.  <br>To be passed as "pause". |
   * | mandate_id | Mandatory. |
   * | pause_start_date | Optional  <br>Date in UNIX EPOCH timestamp (UTC timezone) format, when the
   * mandate will be paused.  <br>If not passed, Default value will be the current date. |
   * | pause_end_date | Mandatory  <br>Date in UNIX EPOCH timestamp (UTC timezone) format, when the
   * mandate status will be automatically changed to ACTIVE. |
   *
   * Note:
   * For pausing a mandate you need to keep the exact timestamp in mind.
   * If a recurring payment is scheduled between the pause_start_date and pause_end_date then it will not
   * be executed.
   *
   * @param accept
   * @param brandId
   * @param body
   * @return Response from the API call
   */
  async pauseMandate(
    accept: string,
    brandId: string,
    body: PauseMandateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PauseMandate>> {
    const req = this.createRequest('POST', '/api/v1/mandate');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      accept: [accept, string()],
      brandId: [brandId, string()],
      body: [body, pauseMandateRequestSchema],
    });
    req.header('Accept', mapped.accept);
    req.header('BrandId', mapped.brandId);
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(pauseMandateSchema, requestOptions);
  }

  /**
   * This endpoint will be used to list all the mandates for a particular customer of a merchant.
   *
   * ### Response **parameters:**
   *
   * 1. **totalDeductedAmount**
   * This is the total amount which is deducted from the user's account.
   *
   * 2. **recurringMandateIds**
   * This object returns a list of all the MIT transactions done for the respective mandate.
   *
   * 3. **nextMandateDate**
   * This is the date when the next recurring payment will happen.
   *
   * @param customerId
   * @param email
   * @param accept
   * @param brandId
   * @return Response from the API call
   */
  async listMandates(
    customerId: string,
    email: string,
    accept: string,
    brandId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success40>> {
    const req = this.createRequest('GET', '/api/v1/mandate');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      customerId: [customerId, string()],
      email: [email, string()],
      accept: [accept, string()],
      brandId: [brandId, string()],
    });
    req.header('Accept', mapped.accept);
    req.header('BrandId', mapped.brandId);
    req.query('customerId', mapped.customerId);
    req.query('email', mapped.email);
    req.authenticate([]);
    return req.callAsJson(success40Schema, requestOptions);
  }

  /**
   * This API tells you about all the details of a recurring payment which occurred
   *
   * ### Mandatory
   *
   * you need to pass the `PurchaseID` in the API URL
   *
   * ### Possible Value of Status
   *
   * | **Status** | **Notes** |
   * | --- | --- |
   * | paid | Transaction Successful |
   * | expired | Purchase has Expired. |
   * | error | Transaction has Failed. |
   *
   * ### Response Examples
   *
   * In the response examples you can see instances of both `Paid` and `Error` Status purchases.
   *
   * ### Error Details
   *
   * If there are any issues on the Purchase you can get more details from the `transaction_data.attempts.
   * error` attribute of the response object
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * Please see the Status Code section for further details.
   *
   * @param purchaseId
   * @return Response from the API call
   */
  async getStatus4(
    purchaseId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success41>> {
    const req = this.createRequest('GET');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ purchaseId: [purchaseId, string()] });
    req.header('Content-Type', 'application/json');
    req.appendTemplatePath`/api/v1/purchases/${mapped.purchaseId}`;
    req.authenticate([]);
    return req.callAsJson(success41Schema, requestOptions);
  }

  /**
   * ## List Saved Cards
   *
   * Retrieve all saved card tokens for a given customer. Returns the last 4 digits of each card and its
   * associated `token_reference`.
   *
   * Use this API to display saved cards to the customer for selection before initiating a payment via
   * the S2S API.
   *
   * ### Query Parameters
   *
   * | Parameter | Type | Required | Description |
   * | --- | --- | --- | --- |
   * | `customerId` | string | Yes | Unique identifier for the customer. Must be URL-encoded if it
   * contains special characters. Email addresses are case-sensitive. |
   * | `brandId` | string | Yes | Brand id of the merchant |
   *
   * ### Response Fields
   *
   * | Field | Type | Description |
   * | --- | --- | --- |
   * | `card_last_four` | string | Last 4 digits of the stored card. Display to the customer for card
   * selection. |
   * | `token_reference` | string | Unique reference for this saved card. Pass this in the S2S payment
   * request or Delete Token request. Do not display to the customer. |
   *
   * ### Notes
   *
   * - The `token_reference` does not expire. It remains valid as long as the saved card has not been
   * deleted.
   *
   * - Show saved cards to the customer using `card_last_four` alongside a card brand icon (Visa /
   * Mastercard).
   *
   * - The `token_reference` should only be stored and used server-side.
   *
   *
   * ### Display Tip
   *
   * Show saved cards using cardlast4 alongside a card brand icon (Visa / Mastercard).
   * The token_reference should only be stored and used server-side.
   *
   * @return Response from the API call
   */
  async listSavedCards(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success46[]>> {
    const req = this.createRequest('GET', '/listSavedCards');
    req.baseUrl('Server 3');
    req.authenticate(false);
    return req.callAsJson(array(success46Schema), requestOptions);
  }

  /**
   * ## Purchase
   *
   * Initiate a payment transaction. This is the standard Paysecure Purchase API.
   *
   * When a customer completes the purchase on the Paysecure checkout page and opts to save their card,
   * Paysecure provisions a network token in the background and generates a `token_reference` linked to
   * the customer.
   *
   * ### Mandatory Parameters
   *
   * | Parameter | Type | Description |
   * | --- | --- | --- |
   * | `client.email` | string | Customer's email address.  <br>Please send it in smallcase |
   * | `client.city` | string | Customer's city. |
   * | `client.country` | string | ISO-3166 Alpha-2 country code, uppercase (e.g., IN, US, SG). |
   * | `client.stateCode` | string | State code, uppercase (e.g., RJ, CA, AL). |
   * | `client.street_address` | string | Customer's street address. |
   * | `client.zip_code` | string | Postal / ZIP code. US format: NNNNN or NNNNN-NNNN. |
   * | `purchase.currency` | string | ISO 4217 currency code (e.g., USD, EUR). Must be enabled on your
   * account. |
   * | `purchase.products` | array | Array of product objects with `name` and `price`. |
   * | `brand_id` | string | Your Paysecure Brand ID from Dashboard. |
   * | `success_redirect` | string | URL to redirect customer after successful payment. |
   * | `failure_redirect` | string | URL to redirect customer after failed payment. |
   *
   * ### Optional Parameters
   *
   * | Parameter | Type | Description |
   * | --- | --- | --- |
   * | `pending_redirect` | string | URL to redirect when payment is pending. |
   * | `success_callback` | string | Server-to-server webhook URL for success. |
   * | `failure_callback` | string | Server-to-server webhook URL for failure. |
   * | `merchantRef` | string | Your internal reference for this transaction. |
   * | `paymentMethod` | string | Pre-select payment method (e.g., VISA). |
   * | `client.phone` | string | Customer's phone number. |
   *
   * ### Key Response Fields
   *
   * | Field | Description |
   * | --- | --- |
   * | `id` | Unique purchase ID. |
   * | `checkout_url` | Redirect customer here to complete payment. Card saving happens during this step.
   * |
   * | `direct_post_url` | URL for S2S payment submission. |
   * | `status` | Purchase status. Initially "created". |
   *
   * #### **Network Tokenisation & Card Saving**
   *
   * When the customer completes payment via `checkout_url` and opts to save their card, Paysecure
   * provisions a network token. The `token_reference` is then available via the **List Saved Cards** API.
   *
   * @param body
   * @return Response from the API call
   */
  async purchase2(
    body: PurchaseRequest3,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success47>> {
    const req = this.createRequest('POST', '/api/v1/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ body: [body, purchaseRequest3Schema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(success47Schema, requestOptions);
  }

  /**
   * ## S2S Payment
   *
   * Process a payment using a saved card's `token_reference`. Paysecure resolves the token reference to
   * the underlying network token and processes the payment securely. If the selected MID does not accept
   * tokens, Paysecure automatically falls back to the card number.
   *
   * ### S2S Input Scenarios
   *
   * The S2S API accepts different input types depending on the scenario:
   *
   * | Scenario | What to Send | What Paysecure Does |
   * | --- | --- | --- |
   * | **New card** (first-time customer) | Full card details | Paysecure tokenises the card. If the MID
   * supports tokens, the transaction uses the token; otherwise, the card number is used. |
   * | **Returning customer** (tokenised with Paysecure) | `token_reference` from List Saved Cards |
   * Paysecure uses the stored token and generates a new cryptogram. Falls back to card number if MID
   * doesn't accept tokens. |
   *
   * ### Request Body
   *
   * | Field | Type | Required | Description |
   * | --- | --- | --- | --- |
   * | `token_reference` | string | Conditional | Token reference from List Saved Cards. Required when
   * paying with a Paysecure-tokenised card. |
   * | `cvc` | string | Yes | Card CVC/CVV. **Required even when** **`token_reference`** **is provided**,
   * for 3DS authentication. |
   * | `remember_card` | string | No | `"on"` to save card, `"off"` to skip. Use `"off"` with saved
   * tokens. |
   * | `remote_ip` | string | Yes | Cardholder's IP address. |
   * | `user_agent` | string | Yes | Browser User-Agent string. |
   * | `accept_header` | string | Yes | Browser Accept header. |
   * | `language` | string | Yes | Browser language (e.g., `"en-US"`). |
   * | `java_enabled` | boolean | Yes | Java enabled in browser. |
   * | `javascript_enabled` | boolean | Yes | JavaScript enabled in browser. |
   * | `color_depth` | integer | Yes | Screen colour depth. |
   * | `utc_offset` | integer | Yes | UTC timezone offset (minutes). |
   * | `screen_width` | integer | Yes | Screen width (px). |
   * | `screen_height` | integer | Yes | Screen height (px). |
   *
   * ### Token Reference in Response
   *
   * - **Scenario 1** (New card via S2S): `token_reference` returned in Check Status and Webhook.
   *
   * - **Scenario 2** (Existing `token_reference`): `token_reference` returned in response and Webhook.
   *
   *
   * ### Important Notes
   *
   * - CVC is **required** even when using a `token_reference`.
   *
   * - All browser fields are required for 3D Secure authentication.
   *
   * - Response follows the standard Paysecure payment response format.
   *
   * @param s2S          Must be "true" for server-to-server requests.
   * @param body
   * @param brandId
   * @return Response from the API call
   */
  async s2SPayment(
    s2S: boolean,
    body: S2SPaymentRequest,
    brandId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success1>> {
    const req = this.createRequest('POST');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      s2S: [s2S, boolean()],
      body: [body, s2SPaymentRequestSchema],
      brandId: [brandId, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('s2s', mapped.s2S);
    req.json(mapped.body);
    req.appendTemplatePath`/api/v1/api/v1/p/${mapped.brandId}`;
    req.authenticate([]);
    return req.callAsJson(success1Schema, requestOptions);
  }

  /**
   * ## Delete Token
   *
   * Permanently delete a saved card and deactivate the associated network token. **This action is
   * irreversible.**
   *
   * ### Path Parameters
   *
   * | Parameter | Type | Required | Description |
   * | --- | --- | --- | --- |
   * | `token_reference` | string | Yes | The `token_reference` of the saved card to delete. Obtained
   * from the List Saved Cards API. |
   *
   * ### Important Notes
   *
   * - Deleting a token is **permanent**. The `token_reference` can no longer be used for payments.
   *
   * - The associated network token is deactivated with the card network.
   *
   * - If the customer wants to pay with this card again, they will need to re-enter their card details.
   *
   *
   * ### When to Delete
   *
   * - Customer explicitly requests removal of their saved card.
   *
   * - GDPR or data retention compliance when a customer closes their account.
   *
   * - Consider implementing a "Remove Card" option in your account management UI.
   *
   * @param tokenReference
   * @return Response from the API call
   */
  async deleteToken(
    tokenReference: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success49>> {
    const req = this.createRequest('DELETE');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      tokenReference: [tokenReference, string()],
    });
    req.appendTemplatePath`/api/v1/tokens/v1/deleteToken/${mapped.tokenReference}`;
    req.throwOn(400, FailureError, 'Bad Request');
    req.authenticate([]);
    return req.callAsJson(success49Schema, requestOptions);
  }

  /**
   * ## Overview
   *
   * The **Original Credit Transaction (OCT)**, commonly referred to as **Push to Card**, enables
   * merchants to facilitate near-instantaneous fund transfers directly to a customer’s eligible Visa or
   * Mastercard debit or credit card. Unlike traditional refund processes that can only return funds from
   * a previous purchase, OCT allows for independent disbursements of funds to a cardholder's account.
   *
   * This feature is designed for high-velocity industries that require frictionless, real-time payouts,
   * such as:
   *
   * - **Gaming & Betting:** Real-time withdrawal of player winnings.
   *
   * - **Marketplaces:** Payouts to sellers or service providers.
   *
   * - **Corporate Disbursements:** Instant insurance claims, rebates, or compensation payments.
   *
   * - **Merchant Settlements:** Faster access to processed funds for sub-merchants.
   *
   *
   * By utilizing the Paysecure `/payout` API with the `PAYOUT-CARDS` method, you benefit from a unified
   * orchestration layer that handles the complexities of card network routing, ensuring high success
   * rates and global reach while maintaining the same security standards as your standard payment
   * integrations.
   *
   * ---
   *
   * ## Integration Steps
   *
   * 1. **Enable OCT:** Ensure "Push to Card" permissions are enabled for your merchant account in the
   * Paysecure Merchant Portal.
   *
   * 2. **Collect Card Details:** Use a secure method to collect the recipient's card number and expiry
   * date or can you Paysecure's cashier to do that.
   *
   * 3. **Execute Payout:** Call the `/payout` endpoint with the OCT-specific payout method.
   *
   * 4. **Handle Webhooks:** Listen for the `paid` or `failure` events to update your internal records.
   *
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Required** | **Notes** |
   * | --- | --- | --- |
   * | client.email | Yes | The customer's email.  <br>Please send it in small case. |
   * | client.phone | Yes | The customer's phone number along with the country code.  <br>Example: +91
   * 9634088651 |
   * | client.city | Yes | The customer's city. |
   * | client.country | Yes | ISO-3166 Country Code. Must be upper case. Example “SG” (Alpha2) |
   * | client.stateCode | Optional | Example “AL”, “XZ”. Must be in upper case. |
   * | Client.street_address | Optional | The customer's address. |
   * | client.zip_code | Optional | The customer's ZIP or postal code. If country=US, zip format must be
   * NNNNN or NNNNN-NNNN. |
   * | currency | Yes | ISO 4217 code for currency you want to send the transaction in.  <br>  <br>Please
   * note, the currency has to be enabled by the account manager for your account. |
   * | amount | Yes | amount in decimal format.  <br>example 1:  <br>EUR 5 , should be sent as 5.00  <br>
   * <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.37 |
   * | payoutMethod | Optional | example: PAYOUT-CARDS |
   * | success_redirect | Yes | URL to send the user if the transactions is successful. |
   * | pending_redirect | Yes | URL to send the user if the transactions is in pending. |
   * | failure_redirect | Yes | URL to send the user if the transactions is unsuccessful. |
   * | Purpose | Optional | The purpose of doing a payout. |
   * | extraParam.token_reference | Yes | Paysecure Token |
   *
   * Note:
   *
   * If the merchant is not PCI DSS compliant then they will need to use our **cashier** and we will show
   * the user all the saved cards for him/her. Based on the card chosen, Paysecure will pick up the
   * token_reference from our Back end.
   *
   * If the merchant is PCI DSS compliant then they can use the payout API and send the token reference
   * as shown above.
   *
   * @param accept
   * @param body
   * @return Response from the API call
   */
  async payOut1(
    accept: string,
    body: PayOutRequest3,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('POST', '/api/v1/payout');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      accept: [accept, string()],
      body: [body, payOutRequest3Schema],
    });
    req.header('Accept', mapped.accept);
    req.json(mapped.body);
    req.throwOn(401, ApiError, 'Unauthorized');
    req.authenticate([]);
    return req.call(requestOptions);
  }

  /**
   * Use this endpoint to authorise a payment without capturing it. This places a temporary hold on the
   * customer’s funds for the specified amount.
   *
   * To Initiate a payment, the very first call to make is `/purchases` with the required data in the
   * request body.
   *
   * To generate a Purchase, you are required to provide the `Brand ID` (in the request body) and `API
   * key` (in the header) Both can be located in the Dashboard section of your merchant account login.
   *
   * The request body structure is shown on the request body of the example request shown here.
   *
   * The following (see table below) are the mandatory parameters that are required to create a purchase
   * request.
   *
   * ### **Mandatory parameters in the request body:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | client.email | The customer's email. |
   * | client.city | The customer's city. |
   * | client.country | ISO-3166 Country Code. Must be upper case. Example “SG” (Alpha2) |
   * | client.stateCode | Example “AL”, “XZ”. Must be in upper case. |
   * | Client.street_address | The customer's address. |
   * | client.zip_code | The customer's ZIP or postal code. If country=US, zip format must be NNNNN or
   * NNNNN-NNNN. |
   * | purchase.currency | ISO 4217 code for currency you want to send the transaction in.  <br>
   * <br>Please note, the currency has to be enabled by the account manager for your account. |
   * | purchase.products | An object which contains the list of products which the customer is buying. |
   * | purchase.products.name | The name of the product. |
   * | purchase.products.price | Price in decimal format.  <br>example 1:  <br>EUR 5 , should be sent as
   * 5.00  <br>  <br>example 2:  <br>USD 10 and 37 cents , should be sent as 10.37 |
   * | brand_id | Obtain from Dashboard section of your merchant account login. |
   * | success_redirect | URL to send the user if the transactions is successful. |
   * | pending_redirect | URL to send the user if the transactions is in pending. |
   * | failure_redirect | URL to send the user if the transactions is unsuccessful. |
   *
   * ### **Additional parameters for this usecase:**
   *
   * | **Parameter** | **Notes** |
   * | --- | --- |
   * | skip_capture | true |
   *
   * ## Essential Optional Parameters
   *
   * ### merchantRef
   *
   * The `merchantRef` parameter, although not mandatory, is strongly advised for merchants to specify.
   * Its inclusion offers two significant advantages for the merchant:
   *
   * 1. **Prevention of Duplicate Requests:** When a `merchantRef` is utilized, it acts as a unique
   * identifier. This means that if a second purchase request is made using the same `merchantRef`
   * (reference number), the system will recognize it and prevent duplicate transactions from occurring.
   * This prevents unintended or duplicate purchases.
   *
   * 2. **Facilitation of Transaction Retrieval:** In situations where the response from the initial
   * purchase request times out or the 'purchaseId' isn't received, having the `merchantRef` allows the
   * merchant to retrieve detailed transaction information. This ensures they can track and access the
   * specific purchase details related to that reference, even if the immediate response was not received.
   *
   *
   * However, if the merchant does not specify the 'merchantRef' parameter, the platform will
   * automatically assign and use an internal 'purchaseId' as a reference for that transaction.
   *
   * Upon successul create on a Purchase you'd get a `"purchaseId"` . Use this ID for the next step in
   * Server to server call by calling `p/{purchaseId}/?s2s=true .`
   *
   * ### paymentMethod
   *
   * The `paymentMethod` parameter identifies which payment solution merchant wants to use to perform a
   * transaction. If parameter is provided, than Paysecure will perform direct payment with the selected
   * payment method instead of loading Paysecure URL.
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * ### Possible Error Messages
   *
   * | **Error Messages** |
   * | --- |
   * | Allowed Limit for this card for particular time period has been consumed |
   * | some Mandatory Parameter are missing |
   * | Invalid format of Date_of_Birth \[allowed format: yyyy-mm-dd\] |
   * | Invalid Email Format |
   * | Please submit Valid Alpha2 Country Code Ex:(AF,IN) in \\"country\\" parameter |
   * | Please pass Valid State Code |
   * | Please pass valid street address in \\"street_address\\" parameter |
   * | Please pass valid city name in \\"city\\" parameter |
   * | Please pass valid postal Code name in \\"zip_code\\" parameter |
   * | You are not Allowed for Live Transaction |
   * | Merchant Limit is not set |
   * | Minimum amount is not set for this merchant |
   * | Brand not found ! |
   * | Your charges setting is incomplete .Plese Contact to Administrator. |
   * | Only {{currencyCode}} currency is allowed. |
   * | Transaction amount must be equal or greater to minimum trans amount |
   * | Invalid_Parameter  <br>You'd usually get it when one or more mandatory parameters are not present
   * in the request. |
   *
   * @param body
   * @return Response from the API call
   */
  async purchase3(
    body: PurchaseRequest4,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Purchase73>> {
    const req = this.createRequest('POST', '/api/v1/purchases');
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ body: [body, purchaseRequest4Schema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([]);
    return req.callAsJson(purchase73Schema, requestOptions);
  }

  /**
   * This API would be the 2nd in sequence to call if the call to `/purchases/` was successful in step 1.
   * and you want to do the transaction via Server-to-Server mode.
   *
   * The request body would contain the details of the card that's to be transacted upon.
   *
   * ### **Mandatory parameters in request body:**
   *
   * | Parameter | **Notes** |
   * | --- | --- |
   * | remote_ip | IP address of the customer’s device used for transaction. Helps in fraud checks and
   * risk scoring. Ex- 157.38.242.7 |
   * | user_agent | Full browser and operating system details captured from HTTP header. EX- Chrome/5.0
   * (X11; Linux x86_64) |
   * | accept_header | Browser’s accepted content types (from HTTP header). Used for validating request
   * origin and device info. EX- text/html |
   *
   * **Note:**
   * If you want to use token_reference for doing a card transaction, please have a look at Network
   * Tokenisation Section.
   *
   * ### **Essential parameters in request body:**
   *
   * | Parameter | **Notes** |
   * | --- | --- |
   * | cardholder_name | Name of the cardholder |
   * | card_number | The card numbers Must be 10-20 characters. |
   * | expires | must be greater than the current month/year. |
   * | cvc | 3 or 4 digit |
   *
   * **
   * Other optional parameters in request body:**
   *
   * | Parameter | **Notes** |
   * | --- | --- |
   * | language | Preferred language of the user’s browser . EX - en-US |
   * | java_enabled | Indicates if Java is enabled in the browser. Helps PSPs in device profiling. Ex-
   * true/false |
   * | javascript_enabled | Indicates if JavaScript is enabled in the client browser. Used for 3DS or
   * risk-based checks. Ex- true |
   * | color_depth | Bit depth of the display screen. Ex- 24 |
   * | utc_offset | Difference in minutes between local time and UTC. Used for location and timezone
   * checks. Ex- 330 |
   * | screen_width | Width of the device screen in pixels. Used in device profiling. Ex- 1920 |
   * | screen_height | Height of the device screen in pixels. Ex- 1080 |
   *
   * ### Successful Response
   *
   * If all the details are correct you'll get a 202 response with staus as Pending
   *
   * ``` json
   * {
   * "status": "pending",
   * "callback_url": "https://paysecure.net/payment/63bd0bf80fb42a076e8a4dd1/",
   * "method": "GET"
   * }
   *
   * ```
   *
   * If the response code is 202, after receiving the response body, direct the customer to the
   * callback_url provided in the response.
   *
   * ### Errors
   *
   * If there are any errors then it'll be in the format of :
   *
   * ``` json
   * {
   * "message": "descriptive error message",
   * "code": "error_code"
   * }
   *
   * ```
   *
   * Please see the Status Code section for further details.
   *
   * | **Error Messages** |
   * | --- |
   * | Client Ip could not be matched with Merchant Ip |
   * | Invalid Card Information |
   * | Card is Blocked |
   * | Different Type of key used to create purchase and payment |
   * | You charges setting is incomplete .Plese Contact to adminstrartor. |
   * | Allowed Attempt for this Transaction has been consumed |
   * | Invalid Card Expiry(Valid Format:MM/YY) must be greator than current month/year |
   * | Customer profile is Blocked |
   * | Customer/Card not allowed for transaction |
   *
   * @param s2S
   * @param body
   * @return Response from the API call
   */
  async s2S1(
    s2S: boolean,
    body: S2Srequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Success1>> {
    const req = this.createRequest(
      'POST',
      '/api/v1/p/64bff4fff63e36669499e9c8'
    );
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({
      s2S: [s2S, boolean()],
      body: [body, s2SrequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.query('s2s', mapped.s2S);
    req.json(mapped.body);
    req.throwOn(401, FailureError, 'Unauthorized');
    req.authenticate([]);
    return req.callAsJson(success1Schema, requestOptions);
  }

  /**
   * After a successful pre-authorisation, use this endpoint to capture (settle) the held funds. You may
   * capture the full authorised amount or a partial amount.
   *
   * @param body
   * @return Response from the API call
   */
  async capture(
    body: CaptureRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest(
      'POST',
      '/api/v1/purchases/69cd26b053d7b783ebf6b6e9/capture'
    );
    req.baseUrl('Server 3');
    const mapped = req.prepareArgs({ body: [body, captureRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(500, CaptureError, 'Internal Server Error');
    req.authenticate([]);
    return req.call(requestOptions);
  }

  /**
   * Use this endpoint to cancel an authorised payment before it has been captured. Voiding releases the
   * held funds back to the customer immediately.
   *
   * @return Response from the API call
   */
  async cancel1(requestOptions?: RequestOptions): Promise<ApiResponse<void>> {
    const req = this.createRequest(
      'POST',
      '/api/v1/purchases/69ca76dbace4fa4413db928c/cancel'
    );
    req.baseUrl('Server 3');
    req.throwOn(405, CancelError, 'Method Not Allowed');
    req.authenticate([]);
    return req.call(requestOptions);
  }
}
